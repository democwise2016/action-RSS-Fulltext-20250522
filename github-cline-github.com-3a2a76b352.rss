<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[cline Release [GH][RSS+]]]></title>
    <link>https://github.com/cline/cline/releases.atom</link>
    <image>
      <url>https://fastly.picsum.photos/id/16/800/800.jpg?hmac=ffLO8oPXECVbAa4FALBtuhSWzX5688S9Cq6C2D2yFUI</url>
      <title>cline Release [GH][RSS+]</title>
      <link>https://github.com/cline/cline/releases.atom</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://github.com/cline/cline/releases.atom" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[cline Release [GH][RSS+]]]></copyright>
    <itunes:author><![CDATA[cline Release [GH][RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      <a href="https://github.com/cline/cline/releases" target="_blank">https://github.com/cline/cline/releases</a><br />
<br />
<a href="https://github.com/cline/cline/releases.atom" target="_blank">https://github.com/cline/cline/releases.atom</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      <a href="https://github.com/cline/cline/releases" target="_blank">https://github.com/cline/cline/releases</a><br />
<br />
<a href="https://github.com/cline/cline/releases.atom" target="_blank">https://github.com/cline/cline/releases.atom</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[cline Release [GH][RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://fastly.picsum.photos/id/16/800/800.jpg?hmac=ffLO8oPXECVbAa4FALBtuhSWzX5688S9Cq6C2D2yFUI"/>
<item>
    <title><![CDATA[tmpfiles暫存檔案託管平台：檔案「title.txt」限時59分鐘下載頁面]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fshared%2Fv0.0.85</link>
    <itunes:title><![CDATA[tmpfiles暫存檔案託管平台：檔案「title.txt」限時59分鐘下載頁面]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/shared@0.0.85</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/shared@0.0.85</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/shared@0.0.85</p>]]></content:encoded>
    
    <pubDate>2026-09-22T08:13:38.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles臨時檔案分享：title.txt開放下載並於59分鐘後過期失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fshared%2Fv0.0.84</link>
    <itunes:title><![CDATA[tmpfiles臨時檔案分享：title.txt開放下載並於59分鐘後過期失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/shared@0.0.84</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/shared@0.0.84</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/shared@0.0.84</p>]]></content:encoded>
    
    <pubDate>2026-09-22T03:26:00.000Z</pubDate>
  </item><item>
    <title><![CDATA[/tmp/files 暫存空間提供 title.txt 下載：檔案將於 59 分鐘後過期失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fsdk%2Fv0.0.85</link>
    <itunes:title><![CDATA[/tmp/files 暫存空間提供 title.txt 下載：檔案將於 59 分鐘後過期失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>A turn that hits the model's output-token limit without producing a usable tool call is now retried instead of failing the run. Reasoning or a large response could consume the whole output allowance before the agent acted, and a single <code>max-tokens</code> finish ended the run with the output-token-limit error. The runtime now retries up to three consecutive times, appending a reminder to respond concisely and split large work across tool calls; the counter resets on tool-call progress and at the start of each run, and exhaustion still fails with the existing error. Empty max-tokens responses — turns that spent the allowance with no visible output — take the same recovery path, and <code>turn-finished</code> is emitted before each recovery iteration so iteration events stay paired</li>
<li>The default output allowance now scales with the model. When neither a request limit nor a caller default is supplied and the model advertises an output limit, the gateway uses <code>max(32000, floor(maxOutputTokens * 0.3))</code> instead of a flat 32,000. The fraction can only raise the default, never lower it, so it changes nothing below roughly a 106,667-token model limit (a 128,000-token model goes from 32,000 to 38,400); the model-output and remaining-context clamps and the reasoning-budget floor still apply, and explicit request limits and caller defaults are handled as before. Longer permitted responses can raise per-turn cost and latency on large-output models</li>
<li>Refreshed the model catalog: still 209 providers, 6,188 to 6,237 models. The Cline Pass tier gains <code>cline-pass/mimo-v2.6-flash</code> and <code>cline-pass/mimo-v2.6-pro</code>. The resolved default model changes for 10 providers that do not pin one in <code>builtins.ts</code> — six land on MiMo V2.6 Flash (Xiaomi and its three token plans, Empirio Labs, NanoGPT), Eden AI moves to Grok 4.7, CoreWeave to DeepSeek V4.1 Flash, CoralBricks to DeepSeek V4.1 Flash Fast, and Tempr from Claude Fable 5.1 to Gemini 3.8 Flash. If you use one of those providers without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.84...sdk/sdk/v0.0.85"><tt>sdk/sdk/v0.0.84...sdk/sdk/v0.0.85</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>A turn that hits the model's output-token limit without producing a usable tool call is now retried instead of failing the run. Reasoning or a large response could consume the whole output allowance before the agent acted, and a single <code>max-tokens</code> finish ended the run with the output-token-limit error. The runtime now retries up to three consecutive times, appending a reminder to respond concisely and split large work across tool calls; the counter resets on tool-call progress and at the start of each run, and exhaustion still fails with the existing error. Empty max-tokens responses — turns that spent the allowance with no visible output — take the same recovery path, and <code>turn-finished</code> is emitted before each recovery iteration so iteration events stay paired</li>
<li>The default output allowance now scales with the model. When neither a request limit nor a caller default is supplied and the model advertises an output limit, the gateway uses <code>max(32000, floor(maxOutputTokens * 0.3))</code> instead of a flat 32,000. The fraction can only raise the default, never lower it, so it changes nothing below roughly a 106,667-token model limit (a 128,000-token model goes from 32,000 to 38,400); the model-output and remaining-context clamps and the reasoning-budget floor still apply, and explicit request limits and caller defaults are handled as before. Longer permitted responses can raise per-turn cost and latency on large-output models</li>
<li>Refreshed the model catalog: still 209 providers, 6,188 to 6,237 models. The Cline Pass tier gains <code>cline-pass/mimo-v2.6-flash</code> and <code>cline-pass/mimo-v2.6-pro</code>. The resolved default model changes for 10 providers that do not pin one in <code>builtins.ts</code> — six land on MiMo V2.6 Flash (Xiaomi and its three token plans, Empirio Labs, NanoGPT), Eden AI moves to Grok 4.7, CoreWeave to DeepSeek V4.1 Flash, CoralBricks to DeepSeek V4.1 Flash Fast, and Tempr from Claude Fable 5.1 to Gemini 3.8 Flash. If you use one of those providers without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.84...sdk/sdk/v0.0.85"><tt>sdk/sdk/v0.0.84...sdk/sdk/v0.0.85</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>A turn that hits the model's output-token limit without producing a usable tool call is now retried instead of failing the run. Reasoning or a large response could consume the whole output allowance before the agent acted, and a single <code>max-tokens</code> finish ended the run with the output-token-limit error. The runtime now retries up to three consecutive times, appending a reminder to respond concisely and split large work across tool calls; the counter resets on tool-call progress and at the start of each run, and exhaustion still fails with the existing error. Empty max-tokens responses — turns that spent the allowance with no visible output — take the same recovery path, and <code>turn-finished</code> is emitted before each recovery iteration so iteration events stay paired</li>
<li>The default output allowance now scales with the model. When neither a request limit nor a caller default is supplied and the model advertises an output limit, the gateway uses <code>max(32000, floor(maxOutputTokens * 0.3))</code> instead of a flat 32,000. The fraction can only raise the default, never lower it, so it changes nothing below roughly a 106,667-token model limit (a 128,000-token model goes from 32,000 to 38,400); the model-output and remaining-context clamps and the reasoning-budget floor still apply, and explicit request limits and caller defaults are handled as before. Longer permitted responses can raise per-turn cost and latency on large-output models</li>
<li>Refreshed the model catalog: still 209 providers, 6,188 to 6,237 models. The Cline Pass tier gains <code>cline-pass/mimo-v2.6-flash</code> and <code>cline-pass/mimo-v2.6-pro</code>. The resolved default model changes for 10 providers that do not pin one in <code>builtins.ts</code> — six land on MiMo V2.6 Flash (Xiaomi and its three token plans, Empirio Labs, NanoGPT), Eden AI moves to Grok 4.7, CoreWeave to DeepSeek V4.1 Flash, CoralBricks to DeepSeek V4.1 Flash Fast, and Tempr from Claude Fable 5.1 to Gemini 3.8 Flash. If you use one of those providers without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.84...sdk/sdk/v0.0.85"><tt>sdk/sdk/v0.0.84...sdk/sdk/v0.0.85</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-22T08:13:58.000Z</pubDate>
  </item><item>
    <title><![CDATA[/tmp/files 暫存檔案分享頁面：檔案 title.txt 提供下載並於 59 分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fsdk%2Fv0.0.84</link>
    <itunes:title><![CDATA[/tmp/files 暫存檔案分享頁面：檔案 title.txt 提供下載並於 59 分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>Concurrent feature-flag polls for the same account now share one in-flight request. The cache was marked fresh before its network request finished, so a caller that raced the initial poll returned immediately with the default value — a flag that was actually enabled read as disabled until something triggered a later refresh. Failures now propagate so a subsequent call can retry, and switching accounts does not wait on the previous account's request</li>
<li>Run-start hooks can now inject context. <code>beforeRun</code> returns a new <code>AgentRunStartResult</code> carrying an <code>appendContext</code> channel alongside <code>stop</code>/<code>reason</code> — <code>AgentStopControl</code> stays assignable, so existing implementors compile unchanged — and the runtime injects what it collects as a single <code>&lt;hook_context source="RunStart"&gt;</code> message placed after the run's input messages, so the model sees it on the run's first request. A resumed session seeded with a trailing unresolved <code>tool_use</code> gets the block immediately before that call instead, keeping the <code>tool_use</code>/<code>tool_result</code> pair adjacent, and a stopping hook's context is never injected. At the hook-file and subprocess layers, <code>agent_start</code> and <code>agent_resume</code> had always spawned detached with stdio ignored, discarding both <code>contextModification</code> and <code>cancel</code> outright; honoring them is now available behind a new <code>blockingRunStartHooks</code> option that defaults to today's fire-and-forget, since blocking by default would silently stall every run start for hosts whose existing scripts linger. With the flag on, a blocking run-start hook that backgrounds a child settles after a post-exit grace period and drops its stdio pipes, so a lingering grandchild can no longer hang the run — hook authors should give backgrounded children their own stdio. A new <code>hooks.detached_runtime</code> event reports each detached hook once, including a censored sample for hooks still running after a 30-second observation window</li>
<li>On Windows, a program planted in the workspace can no longer run in place of the real one. libuv resolves a bare program name (<code>rg</code>, <code>git</code>, <code>powershell</code>, anything the model names) by searching the child's working directory before PATH, and Cline spawns those with the user's repo as cwd, so opening a repo containing an <code>rg.exe</code> executed it during file indexing, before any approval. New <code>disableCurrentDirectoryExecutableSearch()</code> in <code>@cline/shared</code> sets Windows' documented <code>NoDefaultCurrentDirectoryInExePath</code> opt-out, which libuv, Bun 1.4+, cmd.exe and the C runtime all honor, so one call at process startup covers every spawn and every child. Embedders hosting <code>@cline/core</code> in their own Windows process should call it before anything spawns; the Cline CLI, desktop sidecar, VS Code extension and JetBrains core do</li>
<li>Compaction now triggers on the provider's reported input-token count, not just a character estimate. The trigger compared a ~3 chars/token estimate against the model's input budget, so dense content — disassembly, image dumps, minified sources — could reach the real context ceiling while the estimate stayed under the threshold and compaction never fired; affected runs filled the window and had their turns squeezed down to a handful of output tokens. The runtime now records the provider-reported input-token count for each request and threads it to the prepare-turn pipeline as <code>previousRequestInputTokens</code>, and the trigger uses <code>max(estimate, actual)</code>, keeping the estimate as a floor so the very first oversized turn is still caught before any usage has been reported. The default summarizer output budget also goes from 4096 to 8192, since a model that reasons by default can spend a tight budget on thinking and return no summary text at all</li>
<li>Subagent tool calls now run concurrently by default. <code>spawn_agent</code> and configured <code>subagent_&lt;name&gt;</code> tools opt into a new <code>executionMode</code> that <code>createTool</code> preserves and the agent runtime honors, so adjacent parallel calls execute together while sequential tools remain ordering boundaries; unmarked tools keep using the parent's execution setting. Preparation, hooks, and approvals still run sequentially and complete before execution begins, and the parent still waits for every tool result before its next model turn</li>
<li>Configured subagents no longer inherit the parent session's tool approval policies and approval callback for their own tool calls, which aligns them with generic <code>spawn_agent</code> children and teammates. With manual approvals enabled, a configured child prompted for its own tool calls after the parent had already approved the delegation — and once siblings could run concurrently, multiple children reaching the one-shot terminal approval handler on the same stdin meant a single <code>y</code> could approve several pending operations. The parent's approval policy still governs the <code>subagent_&lt;name&gt;</code> delegation call itself, and configured tool allowlists and disabled-tool filtering are preserved</li>
<li>Deleting a session that exists only as an on-disk manifest now actually removes it. History listing can recover a session from its manifest when the session index row is missing, but deletion checked only the index, so the request completed while the manifest survived and the next history refresh showed the session again. Shared session persistence — the path VS Code, JetBrains, and the CLI all use — now deletes valid manifest-backed sessions without rebuilding their missing index rows first, accepting only matching, path-safe session IDs and removing only the canonical session directory under the current Cline data root</li>
<li><code>@cline/shared</code> now exposes rules-path resolvers so hosts stop hardcoding one layout. <code>resolveWorkspaceRulesConfigPaths()</code> returns both supported workspace rule layouts (<code>.clinerules</code> and <code>.cline/rules</code>), and <code>resolveGlobalRulesConfigPaths()</code> returns every global rules location — now including OneDrive Known-Folder-Move Documents candidates, so global rules the VS Code extension created in a redirected Documents folder are finally found by the resolver the desktop app and CLI use</li>
<li>A queued prompt can now be steered to the head of the queue atomically. Core gained a pending-prompt steering path wired through the runtime host and the hub's session handlers, so a client promoting the current queue head cannot race a turn that is already draining, and stale replies are rejected rather than applied late</li>
<li>Requests to the Cline recommended-models endpoint now carry the client type, so recommendations can differ by client. <code>@cline/shared</code> exports a client-identity helper and <code>@cline/llms</code> a matching header builder</li>
<li>Composio connectors, gated behind the account-scoped <code>CLINE_COMPOSIO_BETA</code> feature flag. A new core tools extension registers and executes connector tools through the authenticated Cline API, with the backend owning the Composio project key and scoping connections to the signed-in Cline account. Missing identity or flag configuration denies access, internal email domains do not bypass the gate, and disconnect stays available for cleanup after access is removed. The runtime OAuth token manager also gained a cross-process refresh lock</li>
<li><code>afterModel</code> hooks now receive the <code>requestId</code> of the model call that produced the turn, and core can emit a conversation-linked Git snapshot telemetry event, so a model request can be correlated with the repository state it ran against</li>
<li>Traces no longer lose Langfuse user and session attribution. A dedicated span processor preserves those attributes through the OTLP pipeline</li>
<li>Login-shell PATH probing gets a larger budget, from 2 seconds to 5 seconds per shell. nvm-style profiles regularly need more than 2 seconds on older machines, and a failed probe cascades into missing runtimes for everything the host spawns afterwards</li>
<li>The hub client and run handlers now keep cloud-run state coherent across delayed snapshots and teardown: a settled run is not reopened by late-arriving running metadata regardless of publication order, while genuine queued submissions still restore live running state after prompt deduplication</li>
<li><code>agent-ask-question</code> in <code>@cline/ui</code> submits a single-choice question as soon as an option is picked, instead of requiring a separate Submit step, and gains a free-text input for answers the model did not offer. Multiple-choice questions keep the explicit Submit flow</li>
<li>CoreWeave replaces the Weights &amp; Biases branding, description, and docs link on the <code>wandb</code> provider. The persisted provider ID and stored credentials are unchanged, so existing configurations keep working</li>
<li>Refreshed the model catalog, from 203 to 209 providers and 6,079 to 6,188 models. Kimi For Coding splits into separate kimi.com and kimi.ai providers, replacing the single <code>kimi-for-coding</code> entry; <code>standardcompute</code> is gone; and AI21 Labs, ainetcafe, Inco, OCI Generative AI, Tempr, and Vispark are new. The resolved default model also changes for 29 providers that do not pin one in <code>builtins.ts</code> — eight land on DeepSeek V4.1 Flash (Alibaba CN and its token plan, Nebius, Neuralwatt, SCNet, Synthetic, TensorX, Vancine) and six on GLM 5.3 Flash or FlashX (iteracompute, Privatemode AI, SiliconFlow, Volcengine, ZenMux, Zhipu AI), while GitHub Copilot moves from GPT-6 Astra to Grok 4.7 and StepFun to Step 5 Preview. If you use one of those providers without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.83...sdk/sdk/v0.0.84"><tt>sdk/sdk/v0.0.83...sdk/sdk/v0.0.84</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>Concurrent feature-flag polls for the same account now share one in-flight request. The cache was marked fresh before its network request finished, so a caller that raced the initial poll returned immediately with the default value — a flag that was actually enabled read as disabled until something triggered a later refresh. Failures now propagate so a subsequent call can retry, and switching accounts does not wait on the previous account's request</li>
<li>Run-start hooks can now inject context. <code>beforeRun</code> returns a new <code>AgentRunStartResult</code> carrying an <code>appendContext</code> channel alongside <code>stop</code>/<code>reason</code> — <code>AgentStopControl</code> stays assignable, so existing implementors compile unchanged — and the runtime injects what it collects as a single <code>&lt;hook_context source="RunStart"&gt;</code> message placed after the run's input messages, so the model sees it on the run's first request. A resumed session seeded with a trailing unresolved <code>tool_use</code> gets the block immediately before that call instead, keeping the <code>tool_use</code>/<code>tool_result</code> pair adjacent, and a stopping hook's context is never injected. At the hook-file and subprocess layers, <code>agent_start</code> and <code>agent_resume</code> had always spawned detached with stdio ignored, discarding both <code>contextModification</code> and <code>cancel</code> outright; honoring them is now available behind a new <code>blockingRunStartHooks</code> option that defaults to today's fire-and-forget, since blocking by default would silently stall every run start for hosts whose existing scripts linger. With the flag on, a blocking run-start hook that backgrounds a child settles after a post-exit grace period and drops its stdio pipes, so a lingering grandchild can no longer hang the run — hook authors should give backgrounded children their own stdio. A new <code>hooks.detached_runtime</code> event reports each detached hook once, including a censored sample for hooks still running after a 30-second observation window</li>
<li>On Windows, a program planted in the workspace can no longer run in place of the real one. libuv resolves a bare program name (<code>rg</code>, <code>git</code>, <code>powershell</code>, anything the model names) by searching the child's working directory before PATH, and Cline spawns those with the user's repo as cwd, so opening a repo containing an <code>rg.exe</code> executed it during file indexing, before any approval. New <code>disableCurrentDirectoryExecutableSearch()</code> in <code>@cline/shared</code> sets Windows' documented <code>NoDefaultCurrentDirectoryInExePath</code> opt-out, which libuv, Bun 1.4+, cmd.exe and the C runtime all honor, so one call at process startup covers every spawn and every child. Embedders hosting <code>@cline/core</code> in their own Windows process should call it before anything spawns; the Cline CLI, desktop sidecar, VS Code extension and JetBrains core do</li>
<li>Compaction now triggers on the provider's reported input-token count, not just a character estimate. The trigger compared a ~3 chars/token estimate against the model's input budget, so dense content — disassembly, image dumps, minified sources — could reach the real context ceiling while the estimate stayed under the threshold and compaction never fired; affected runs filled the window and had their turns squeezed down to a handful of output tokens. The runtime now records the provider-reported input-token count for each request and threads it to the prepare-turn pipeline as <code>previousRequestInputTokens</code>, and the trigger uses <code>max(estimate, actual)</code>, keeping the estimate as a floor so the very first oversized turn is still caught before any usage has been reported. The default summarizer output budget also goes from 4096 to 8192, since a model that reasons by default can spend a tight budget on thinking and return no summary text at all</li>
<li>Subagent tool calls now run concurrently by default. <code>spawn_agent</code> and configured <code>subagent_&lt;name&gt;</code> tools opt into a new <code>executionMode</code> that <code>createTool</code> preserves and the agent runtime honors, so adjacent parallel calls execute together while sequential tools remain ordering boundaries; unmarked tools keep using the parent's execution setting. Preparation, hooks, and approvals still run sequentially and complete before execution begins, and the parent still waits for every tool result before its next model turn</li>
<li>Configured subagents no longer inherit the parent session's tool approval policies and approval callback for their own tool calls, which aligns them with generic <code>spawn_agent</code> children and teammates. With manual approvals enabled, a configured child prompted for its own tool calls after the parent had already approved the delegation — and once siblings could run concurrently, multiple children reaching the one-shot terminal approval handler on the same stdin meant a single <code>y</code> could approve several pending operations. The parent's approval policy still governs the <code>subagent_&lt;name&gt;</code> delegation call itself, and configured tool allowlists and disabled-tool filtering are preserved</li>
<li>Deleting a session that exists only as an on-disk manifest now actually removes it. History listing can recover a session from its manifest when the session index row is missing, but deletion checked only the index, so the request completed while the manifest survived and the next history refresh showed the session again. Shared session persistence — the path VS Code, JetBrains, and the CLI all use — now deletes valid manifest-backed sessions without rebuilding their missing index rows first, accepting only matching, path-safe session IDs and removing only the canonical session directory under the current Cline data root</li>
<li><code>@cline/shared</code> now exposes rules-path resolvers so hosts stop hardcoding one layout. <code>resolveWorkspaceRulesConfigPaths()</code> returns both supported workspace rule layouts (<code>.clinerules</code> and <code>.cline/rules</code>), and <code>resolveGlobalRulesConfigPaths()</code> returns every global rules location — now including OneDrive Known-Folder-Move Documents candidates, so global rules the VS Code extension created in a redirected Documents folder are finally found by the resolver the desktop app and CLI use</li>
<li>A queued prompt can now be steered to the head of the queue atomically. Core gained a pending-prompt steering path wired through the runtime host and the hub's session handlers, so a client promoting the current queue head cannot race a turn that is already draining, and stale replies are rejected rather than applied late</li>
<li>Requests to the Cline recommended-models endpoint now carry the client type, so recommendations can differ by client. <code>@cline/shared</code> exports a client-identity helper and <code>@cline/llms</code> a matching header builder</li>
<li>Composio connectors, gated behind the account-scoped <code>CLINE_COMPOSIO_BETA</code> feature flag. A new core tools extension registers and executes connector tools through the authenticated Cline API, with the backend owning the Composio project key and scoping connections to the signed-in Cline account. Missing identity or flag configuration denies access, internal email domains do not bypass the gate, and disconnect stays available for cleanup after access is removed. The runtime OAuth token manager also gained a cross-process refresh lock</li>
<li><code>afterModel</code> hooks now receive the <code>requestId</code> of the model call that produced the turn, and core can emit a conversation-linked Git snapshot telemetry event, so a model request can be correlated with the repository state it ran against</li>
<li>Traces no longer lose Langfuse user and session attribution. A dedicated span processor preserves those attributes through the OTLP pipeline</li>
<li>Login-shell PATH probing gets a larger budget, from 2 seconds to 5 seconds per shell. nvm-style profiles regularly need more than 2 seconds on older machines, and a failed probe cascades into missing runtimes for everything the host spawns afterwards</li>
<li>The hub client and run handlers now keep cloud-run state coherent across delayed snapshots and teardown: a settled run is not reopened by late-arriving running metadata regardless of publication order, while genuine queued submissions still restore live running state after prompt deduplication</li>
<li><code>agent-ask-question</code> in <code>@cline/ui</code> submits a single-choice question as soon as an option is picked, instead of requiring a separate Submit step, and gains a free-text input for answers the model did not offer. Multiple-choice questions keep the explicit Submit flow</li>
<li>CoreWeave replaces the Weights &amp; Biases branding, description, and docs link on the <code>wandb</code> provider. The persisted provider ID and stored credentials are unchanged, so existing configurations keep working</li>
<li>Refreshed the model catalog, from 203 to 209 providers and 6,079 to 6,188 models. Kimi For Coding splits into separate kimi.com and kimi.ai providers, replacing the single <code>kimi-for-coding</code> entry; <code>standardcompute</code> is gone; and AI21 Labs, ainetcafe, Inco, OCI Generative AI, Tempr, and Vispark are new. The resolved default model also changes for 29 providers that do not pin one in <code>builtins.ts</code> — eight land on DeepSeek V4.1 Flash (Alibaba CN and its token plan, Nebius, Neuralwatt, SCNet, Synthetic, TensorX, Vancine) and six on GLM 5.3 Flash or FlashX (iteracompute, Privatemode AI, SiliconFlow, Volcengine, ZenMux, Zhipu AI), while GitHub Copilot moves from GPT-6 Astra to Grok 4.7 and StepFun to Step 5 Preview. If you use one of those providers without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.83...sdk/sdk/v0.0.84"><tt>sdk/sdk/v0.0.83...sdk/sdk/v0.0.84</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>Concurrent feature-flag polls for the same account now share one in-flight request. The cache was marked fresh before its network request finished, so a caller that raced the initial poll returned immediately with the default value — a flag that was actually enabled read as disabled until something triggered a later refresh. Failures now propagate so a subsequent call can retry, and switching accounts does not wait on the previous account's request</li>
<li>Run-start hooks can now inject context. <code>beforeRun</code> returns a new <code>AgentRunStartResult</code> carrying an <code>appendContext</code> channel alongside <code>stop</code>/<code>reason</code> — <code>AgentStopControl</code> stays assignable, so existing implementors compile unchanged — and the runtime injects what it collects as a single <code>&lt;hook_context source="RunStart"&gt;</code> message placed after the run's input messages, so the model sees it on the run's first request. A resumed session seeded with a trailing unresolved <code>tool_use</code> gets the block immediately before that call instead, keeping the <code>tool_use</code>/<code>tool_result</code> pair adjacent, and a stopping hook's context is never injected. At the hook-file and subprocess layers, <code>agent_start</code> and <code>agent_resume</code> had always spawned detached with stdio ignored, discarding both <code>contextModification</code> and <code>cancel</code> outright; honoring them is now available behind a new <code>blockingRunStartHooks</code> option that defaults to today's fire-and-forget, since blocking by default would silently stall every run start for hosts whose existing scripts linger. With the flag on, a blocking run-start hook that backgrounds a child settles after a post-exit grace period and drops its stdio pipes, so a lingering grandchild can no longer hang the run — hook authors should give backgrounded children their own stdio. A new <code>hooks.detached_runtime</code> event reports each detached hook once, including a censored sample for hooks still running after a 30-second observation window</li>
<li>On Windows, a program planted in the workspace can no longer run in place of the real one. libuv resolves a bare program name (<code>rg</code>, <code>git</code>, <code>powershell</code>, anything the model names) by searching the child's working directory before PATH, and Cline spawns those with the user's repo as cwd, so opening a repo containing an <code>rg.exe</code> executed it during file indexing, before any approval. New <code>disableCurrentDirectoryExecutableSearch()</code> in <code>@cline/shared</code> sets Windows' documented <code>NoDefaultCurrentDirectoryInExePath</code> opt-out, which libuv, Bun 1.4+, cmd.exe and the C runtime all honor, so one call at process startup covers every spawn and every child. Embedders hosting <code>@cline/core</code> in their own Windows process should call it before anything spawns; the Cline CLI, desktop sidecar, VS Code extension and JetBrains core do</li>
<li>Compaction now triggers on the provider's reported input-token count, not just a character estimate. The trigger compared a ~3 chars/token estimate against the model's input budget, so dense content — disassembly, image dumps, minified sources — could reach the real context ceiling while the estimate stayed under the threshold and compaction never fired; affected runs filled the window and had their turns squeezed down to a handful of output tokens. The runtime now records the provider-reported input-token count for each request and threads it to the prepare-turn pipeline as <code>previousRequestInputTokens</code>, and the trigger uses <code>max(estimate, actual)</code>, keeping the estimate as a floor so the very first oversized turn is still caught before any usage has been reported. The default summarizer output budget also goes from 4096 to 8192, since a model that reasons by default can spend a tight budget on thinking and return no summary text at all</li>
<li>Subagent tool calls now run concurrently by default. <code>spawn_agent</code> and configured <code>subagent_&lt;name&gt;</code> tools opt into a new <code>executionMode</code> that <code>createTool</code> preserves and the agent runtime honors, so adjacent parallel calls execute together while sequential tools remain ordering boundaries; unmarked tools keep using the parent's execution setting. Preparation, hooks, and approvals still run sequentially and complete before execution begins, and the parent still waits for every tool result before its next model turn</li>
<li>Configured subagents no longer inherit the parent session's tool approval policies and approval callback for their own tool calls, which aligns them with generic <code>spawn_agent</code> children and teammates. With manual approvals enabled, a configured child prompted for its own tool calls after the parent had already approved the delegation — and once siblings could run concurrently, multiple children reaching the one-shot terminal approval handler on the same stdin meant a single <code>y</code> could approve several pending operations. The parent's approval policy still governs the <code>subagent_&lt;name&gt;</code> delegation call itself, and configured tool allowlists and disabled-tool filtering are preserved</li>
<li>Deleting a session that exists only as an on-disk manifest now actually removes it. History listing can recover a session from its manifest when the session index row is missing, but deletion checked only the index, so the request completed while the manifest survived and the next history refresh showed the session again. Shared session persistence — the path VS Code, JetBrains, and the CLI all use — now deletes valid manifest-backed sessions without rebuilding their missing index rows first, accepting only matching, path-safe session IDs and removing only the canonical session directory under the current Cline data root</li>
<li><code>@cline/shared</code> now exposes rules-path resolvers so hosts stop hardcoding one layout. <code>resolveWorkspaceRulesConfigPaths()</code> returns both supported workspace rule layouts (<code>.clinerules</code> and <code>.cline/rules</code>), and <code>resolveGlobalRulesConfigPaths()</code> returns every global rules location — now including OneDrive Known-Folder-Move Documents candidates, so global rules the VS Code extension created in a redirected Documents folder are finally found by the resolver the desktop app and CLI use</li>
<li>A queued prompt can now be steered to the head of the queue atomically. Core gained a pending-prompt steering path wired through the runtime host and the hub's session handlers, so a client promoting the current queue head cannot race a turn that is already draining, and stale replies are rejected rather than applied late</li>
<li>Requests to the Cline recommended-models endpoint now carry the client type, so recommendations can differ by client. <code>@cline/shared</code> exports a client-identity helper and <code>@cline/llms</code> a matching header builder</li>
<li>Composio connectors, gated behind the account-scoped <code>CLINE_COMPOSIO_BETA</code> feature flag. A new core tools extension registers and executes connector tools through the authenticated Cline API, with the backend owning the Composio project key and scoping connections to the signed-in Cline account. Missing identity or flag configuration denies access, internal email domains do not bypass the gate, and disconnect stays available for cleanup after access is removed. The runtime OAuth token manager also gained a cross-process refresh lock</li>
<li><code>afterModel</code> hooks now receive the <code>requestId</code> of the model call that produced the turn, and core can emit a conversation-linked Git snapshot telemetry event, so a model request can be correlated with the repository state it ran against</li>
<li>Traces no longer lose Langfuse user and session attribution. A dedicated span processor preserves those attributes through the OTLP pipeline</li>
<li>Login-shell PATH probing gets a larger budget, from 2 seconds to 5 seconds per shell. nvm-style profiles regularly need more than 2 seconds on older machines, and a failed probe cascades into missing runtimes for everything the host spawns afterwards</li>
<li>The hub client and run handlers now keep cloud-run state coherent across delayed snapshots and teardown: a settled run is not reopened by late-arriving running metadata regardless of publication order, while genuine queued submissions still restore live running state after prompt deduplication</li>
<li><code>agent-ask-question</code> in <code>@cline/ui</code> submits a single-choice question as soon as an option is picked, instead of requiring a separate Submit step, and gains a free-text input for answers the model did not offer. Multiple-choice questions keep the explicit Submit flow</li>
<li>CoreWeave replaces the Weights &amp; Biases branding, description, and docs link on the <code>wandb</code> provider. The persisted provider ID and stored credentials are unchanged, so existing configurations keep working</li>
<li>Refreshed the model catalog, from 203 to 209 providers and 6,079 to 6,188 models. Kimi For Coding splits into separate kimi.com and kimi.ai providers, replacing the single <code>kimi-for-coding</code> entry; <code>standardcompute</code> is gone; and AI21 Labs, ainetcafe, Inco, OCI Generative AI, Tempr, and Vispark are new. The resolved default model also changes for 29 providers that do not pin one in <code>builtins.ts</code> — eight land on DeepSeek V4.1 Flash (Alibaba CN and its token plan, Nebius, Neuralwatt, SCNet, Synthetic, TensorX, Vancine) and six on GLM 5.3 Flash or FlashX (iteracompute, Privatemode AI, SiliconFlow, Volcengine, ZenMux, Zhipu AI), while GitHub Copilot moves from GPT-6 Astra to Grok 4.7 and StepFun to Step 5 Preview. If you use one of those providers without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.83...sdk/sdk/v0.0.84"><tt>sdk/sdk/v0.0.83...sdk/sdk/v0.0.84</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-22T03:26:20.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles暫存檔案託管服務：title.txt下載頁面與59分鐘失效倒數]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fllms%2Fv0.0.85</link>
    <itunes:title><![CDATA[tmpfiles暫存檔案託管服務：title.txt下載頁面與59分鐘失效倒數]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/llms@0.0.85</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/llms@0.0.85</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/llms@0.0.85</p>]]></content:encoded>
    
    <pubDate>2026-09-22T08:13:40.000Z</pubDate>
  </item><item>
    <title><![CDATA[暫存檔案平台 tmpfiles 提供 title.txt 下載　連結將於 59 分鐘後過期]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fcore%2Fv0.0.85</link>
    <itunes:title><![CDATA[暫存檔案平台 tmpfiles 提供 title.txt 下載　連結將於 59 分鐘後過期]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/core@0.0.85</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/core@0.0.85</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/core@0.0.85</p>]]></content:encoded>
    
    <pubDate>2026-09-22T08:13:44.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles暫時檔案託管平台開放「title.txt」下載 限時59分鐘後過期]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fcore%2Fv0.0.84</link>
    <itunes:title><![CDATA[tmpfiles暫時檔案託管平台開放「title.txt」下載 限時59分鐘後過期]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/core@0.0.84</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/core@0.0.84</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/core@0.0.84</p>]]></content:encoded>
    
    <pubDate>2026-09-22T03:26:08.000Z</pubDate>
  </item><item>
    <title><![CDATA[/tmp/files臨時檔案託管頁面：title.txt開放下載且將於59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fagents%2Fv0.0.84</link>
    <itunes:title><![CDATA[/tmp/files臨時檔案託管頁面：title.txt開放下載且將於59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/agents@0.0.84</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/agents@0.0.84</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/agents@0.0.84</p>]]></content:encoded>
    
    <pubDate>2026-09-22T03:26:06.000Z</pubDate>
  </item></channel>
</rss>