<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[cline Release [GH][RSS+]]]></title>
    <link>https://github.com/cline/cline/releases.atom</link>
    <image>
      <url>https://fastly.picsum.photos/id/87/800/800.jpg?hmac=XNvXEr7-yfwfCI-aFAvhCslNktgg7by5hrs7BCvaDwc</url>
      <title>cline Release [GH][RSS+]</title>
      <link>https://github.com/cline/cline/releases.atom</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://github.com/cline/cline/releases.atom" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[cline Release [GH][RSS+]]]></copyright>
    <itunes:author><![CDATA[cline Release [GH][RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      <a href="https://github.com/cline/cline/releases" target="_blank">https://github.com/cline/cline/releases</a><br />
<br />
<a href="https://github.com/cline/cline/releases.atom" target="_blank">https://github.com/cline/cline/releases.atom</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      <a href="https://github.com/cline/cline/releases" target="_blank">https://github.com/cline/cline/releases</a><br />
<br />
<a href="https://github.com/cline/cline/releases.atom" target="_blank">https://github.com/cline/cline/releases.atom</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[cline Release [GH][RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://fastly.picsum.photos/id/87/800/800.jpg?hmac=XNvXEr7-yfwfCI-aFAvhCslNktgg7by5hrs7BCvaDwc"/>
<item>
    <title><![CDATA[tmpfiles暫存檔案託管：title.txt（5.92KB）開放下載 59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/desktop-v0.0.26</link>
    <itunes:title><![CDATA[tmpfiles暫存檔案託管：title.txt（5.92KB）開放下載 59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>The composer now shows the current branch's GitHub pull request — PR number, merge status, changed-line totals, and CI checks. Click through to open it in your browser, or expand CI to inspect individual checks and their logs; status refreshes every 30 seconds while visible, on window focus, and on demand. If the branch has no PR, <strong>Create PR</strong> opens GitHub's comparison form. Requires the GitHub CLI (<code>gh</code>) installed and signed in, plus a GitHub.com <code>origin</code> remote; the row hides itself on the default branch, detached HEAD, and unsupported repositories. Cline does not push commits or submit the PR for you</li>
<li>The Customize view's Tools, Skills, and Rules tabs now read as one consistent list instead of three different ones, matching the pattern Plugins already used. Tools gets a search bar that filters both sections and per-section Enable all/Disable all that only touches what's visible; Skills gets an in-place enable/disable toggle — previously the desktop app had no concept of a disabled skill, so disabled ones were hidden and unreachable — and the same Copy path / Uninstall menu Plugins use. Tabs are reordered to Tools, Plugins, Skills, Rules, MCP, Hooks and open on Tools, and the sidebar's "New Task" button is now "New Session"</li>
<li>Deleting a queued prompt no longer leaves it in the transcript as a message that never ran. The sidecar inferred a queued prompt had started whenever the pending list shrank and its head changed — which is exactly what deleting the first queued prompt (or discarding the queue) looks like. It now relies only on the runtime's real start event</li>
<li>A session forked from a checkpoint restore no longer comes back stuck on "Thinking...". Sessions that materialize with seeded history never passed their status when persisting, so the row and manifest were written as "running" while the live session was idle; resuming from that manifest then showed a session that was never going to finish</li>
<li>Sending an image with no text no longer fails with "session input requires a prompt string" — the composer asks for a message instead of letting the request through to a backend that rejects it</li>
<li>Image attachments in formats the model pipeline can't read (HEIC, TIFF, SVG, BMP, ICO, and friends) are now rejected at attach time — by picker, paste, or drag-and-drop — instead of failing later in the turn. Files whose type has to be inferred from the extension are classified the same way</li>
<li>Signing out of ChatGPT (Codex) now sticks. Sign-out removes the provider entry, but the runtime re-imports any missing provider from the classic extension's stored credentials on every command, so the next action signed you straight back in. The legacy Codex credentials are now cleared too, and a failed clear reports the sign-out as failed instead of quietly succeeding</li>
<li>Auth failures on providers that log in through a local CLI — Claude Code, Codex CLI, OpenCode — now point you at that CLI instead of Settings → Models, where there is nothing to fix. "OAuth session expired", "Not logged in · Please run /login", and similar messages are also recognized as credential failures now, so they get a hint at all</li>
<li>Model lists now refresh from the live catalog for every provider that uses the shared catalog, not just Cline and Cline Pass. Providers with their own endpoint-owned model lists are unaffected, and the first providers listing is still network-free</li>
<li>Cline Pass and free models now report zero cost instead of the upstream provider's price</li>
<li>Session history rows no longer overlap their own hover metadata when a label is wider than the fixed label column</li>
<li>Scheduled sessions no longer stall out. The poller could stop advancing, and capacity waits were counted as run attempts, so a schedule could burn through its retries without ever running. Execution lifecycle and capacity claims are now fenced atomically. New recurring schedules also default to your local timezone instead of UTC; existing schedules without a timezone keep it that way when edited</li>
<li>Automation event acceptance is now atomic and retryable, so an event can't be half-accepted and lost if delivery fails mid-way</li>
<li>Nested PowerShell commands no longer flood errors and look like a hang. Commands run through an outer PowerShell bootstrap, so a nested <code>powershell -Command "... $_ ..."</code> had <code>$_</code> interpolated away by the outer parser before the inner shell saw it — a <code>Where-Object { $_.Name ... }</code> pipeline then errored once per item over a large tree while still exiting 0. Redundant nested invocations are now unwrapped and run directly, only when that provably preserves semantics (same PowerShell edition, no profile loading, fully quoted command). Cline is also told which edition it's actually on — Windows PowerShell vs. Microsoft PowerShell — and to stop wrapping commands in a shell it's already running in</li>
<li>Prompt telemetry to Cline's tracing backend is now limited to Cline and Cline Pass requests. Turns run against your own provider keys are not traced</li>
<li>Cline Pass now appears in the composer's provider picker alongside Cline. One Cline sign-in configures both — Cline Pass stores its credentials under the Cline account rather than having its own — but the picker only listed providers with their own saved settings entry, and onboarding writes just the Cline one. A new Cline Pass user therefore saw a single row</li>
<li>The composer's provider picker has a <strong>Set up another provider</strong> row at the bottom that opens Settings → Models. The picker only lists what you have already configured, so there was no way to reach the rest of the catalog from the composer</li>
<li>Dropped the "Configured" checkmark from the composer's provider picker (added in 0.0.25). Every row in that picker has a saved settings entry, so for anyone who set their providers up in the app every row carried the same green check and it read as decoration. Settings is still where provider readiness is shown, and it distinguishes a real credential from an entry a legacy migration left behind</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.25...desktop-v0.0.26"><tt>desktop-v0.0.25...desktop-v0.0.26</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>The composer now shows the current branch's GitHub pull request — PR number, merge status, changed-line totals, and CI checks. Click through to open it in your browser, or expand CI to inspect individual checks and their logs; status refreshes every 30 seconds while visible, on window focus, and on demand. If the branch has no PR, <strong>Create PR</strong> opens GitHub's comparison form. Requires the GitHub CLI (<code>gh</code>) installed and signed in, plus a GitHub.com <code>origin</code> remote; the row hides itself on the default branch, detached HEAD, and unsupported repositories. Cline does not push commits or submit the PR for you</li>
<li>The Customize view's Tools, Skills, and Rules tabs now read as one consistent list instead of three different ones, matching the pattern Plugins already used. Tools gets a search bar that filters both sections and per-section Enable all/Disable all that only touches what's visible; Skills gets an in-place enable/disable toggle — previously the desktop app had no concept of a disabled skill, so disabled ones were hidden and unreachable — and the same Copy path / Uninstall menu Plugins use. Tabs are reordered to Tools, Plugins, Skills, Rules, MCP, Hooks and open on Tools, and the sidebar's "New Task" button is now "New Session"</li>
<li>Deleting a queued prompt no longer leaves it in the transcript as a message that never ran. The sidecar inferred a queued prompt had started whenever the pending list shrank and its head changed — which is exactly what deleting the first queued prompt (or discarding the queue) looks like. It now relies only on the runtime's real start event</li>
<li>A session forked from a checkpoint restore no longer comes back stuck on "Thinking...". Sessions that materialize with seeded history never passed their status when persisting, so the row and manifest were written as "running" while the live session was idle; resuming from that manifest then showed a session that was never going to finish</li>
<li>Sending an image with no text no longer fails with "session input requires a prompt string" — the composer asks for a message instead of letting the request through to a backend that rejects it</li>
<li>Image attachments in formats the model pipeline can't read (HEIC, TIFF, SVG, BMP, ICO, and friends) are now rejected at attach time — by picker, paste, or drag-and-drop — instead of failing later in the turn. Files whose type has to be inferred from the extension are classified the same way</li>
<li>Signing out of ChatGPT (Codex) now sticks. Sign-out removes the provider entry, but the runtime re-imports any missing provider from the classic extension's stored credentials on every command, so the next action signed you straight back in. The legacy Codex credentials are now cleared too, and a failed clear reports the sign-out as failed instead of quietly succeeding</li>
<li>Auth failures on providers that log in through a local CLI — Claude Code, Codex CLI, OpenCode — now point you at that CLI instead of Settings → Models, where there is nothing to fix. "OAuth session expired", "Not logged in · Please run /login", and similar messages are also recognized as credential failures now, so they get a hint at all</li>
<li>Model lists now refresh from the live catalog for every provider that uses the shared catalog, not just Cline and Cline Pass. Providers with their own endpoint-owned model lists are unaffected, and the first providers listing is still network-free</li>
<li>Cline Pass and free models now report zero cost instead of the upstream provider's price</li>
<li>Session history rows no longer overlap their own hover metadata when a label is wider than the fixed label column</li>
<li>Scheduled sessions no longer stall out. The poller could stop advancing, and capacity waits were counted as run attempts, so a schedule could burn through its retries without ever running. Execution lifecycle and capacity claims are now fenced atomically. New recurring schedules also default to your local timezone instead of UTC; existing schedules without a timezone keep it that way when edited</li>
<li>Automation event acceptance is now atomic and retryable, so an event can't be half-accepted and lost if delivery fails mid-way</li>
<li>Nested PowerShell commands no longer flood errors and look like a hang. Commands run through an outer PowerShell bootstrap, so a nested <code>powershell -Command "... $_ ..."</code> had <code>$_</code> interpolated away by the outer parser before the inner shell saw it — a <code>Where-Object { $_.Name ... }</code> pipeline then errored once per item over a large tree while still exiting 0. Redundant nested invocations are now unwrapped and run directly, only when that provably preserves semantics (same PowerShell edition, no profile loading, fully quoted command). Cline is also told which edition it's actually on — Windows PowerShell vs. Microsoft PowerShell — and to stop wrapping commands in a shell it's already running in</li>
<li>Prompt telemetry to Cline's tracing backend is now limited to Cline and Cline Pass requests. Turns run against your own provider keys are not traced</li>
<li>Cline Pass now appears in the composer's provider picker alongside Cline. One Cline sign-in configures both — Cline Pass stores its credentials under the Cline account rather than having its own — but the picker only listed providers with their own saved settings entry, and onboarding writes just the Cline one. A new Cline Pass user therefore saw a single row</li>
<li>The composer's provider picker has a <strong>Set up another provider</strong> row at the bottom that opens Settings → Models. The picker only lists what you have already configured, so there was no way to reach the rest of the catalog from the composer</li>
<li>Dropped the "Configured" checkmark from the composer's provider picker (added in 0.0.25). Every row in that picker has a saved settings entry, so for anyone who set their providers up in the app every row carried the same green check and it read as decoration. Settings is still where provider readiness is shown, and it distinguishes a real credential from an entry a legacy migration left behind</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.25...desktop-v0.0.26"><tt>desktop-v0.0.25...desktop-v0.0.26</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>The composer now shows the current branch's GitHub pull request — PR number, merge status, changed-line totals, and CI checks. Click through to open it in your browser, or expand CI to inspect individual checks and their logs; status refreshes every 30 seconds while visible, on window focus, and on demand. If the branch has no PR, <strong>Create PR</strong> opens GitHub's comparison form. Requires the GitHub CLI (<code>gh</code>) installed and signed in, plus a GitHub.com <code>origin</code> remote; the row hides itself on the default branch, detached HEAD, and unsupported repositories. Cline does not push commits or submit the PR for you</li>
<li>The Customize view's Tools, Skills, and Rules tabs now read as one consistent list instead of three different ones, matching the pattern Plugins already used. Tools gets a search bar that filters both sections and per-section Enable all/Disable all that only touches what's visible; Skills gets an in-place enable/disable toggle — previously the desktop app had no concept of a disabled skill, so disabled ones were hidden and unreachable — and the same Copy path / Uninstall menu Plugins use. Tabs are reordered to Tools, Plugins, Skills, Rules, MCP, Hooks and open on Tools, and the sidebar's "New Task" button is now "New Session"</li>
<li>Deleting a queued prompt no longer leaves it in the transcript as a message that never ran. The sidecar inferred a queued prompt had started whenever the pending list shrank and its head changed — which is exactly what deleting the first queued prompt (or discarding the queue) looks like. It now relies only on the runtime's real start event</li>
<li>A session forked from a checkpoint restore no longer comes back stuck on "Thinking...". Sessions that materialize with seeded history never passed their status when persisting, so the row and manifest were written as "running" while the live session was idle; resuming from that manifest then showed a session that was never going to finish</li>
<li>Sending an image with no text no longer fails with "session input requires a prompt string" — the composer asks for a message instead of letting the request through to a backend that rejects it</li>
<li>Image attachments in formats the model pipeline can't read (HEIC, TIFF, SVG, BMP, ICO, and friends) are now rejected at attach time — by picker, paste, or drag-and-drop — instead of failing later in the turn. Files whose type has to be inferred from the extension are classified the same way</li>
<li>Signing out of ChatGPT (Codex) now sticks. Sign-out removes the provider entry, but the runtime re-imports any missing provider from the classic extension's stored credentials on every command, so the next action signed you straight back in. The legacy Codex credentials are now cleared too, and a failed clear reports the sign-out as failed instead of quietly succeeding</li>
<li>Auth failures on providers that log in through a local CLI — Claude Code, Codex CLI, OpenCode — now point you at that CLI instead of Settings → Models, where there is nothing to fix. "OAuth session expired", "Not logged in · Please run /login", and similar messages are also recognized as credential failures now, so they get a hint at all</li>
<li>Model lists now refresh from the live catalog for every provider that uses the shared catalog, not just Cline and Cline Pass. Providers with their own endpoint-owned model lists are unaffected, and the first providers listing is still network-free</li>
<li>Cline Pass and free models now report zero cost instead of the upstream provider's price</li>
<li>Session history rows no longer overlap their own hover metadata when a label is wider than the fixed label column</li>
<li>Scheduled sessions no longer stall out. The poller could stop advancing, and capacity waits were counted as run attempts, so a schedule could burn through its retries without ever running. Execution lifecycle and capacity claims are now fenced atomically. New recurring schedules also default to your local timezone instead of UTC; existing schedules without a timezone keep it that way when edited</li>
<li>Automation event acceptance is now atomic and retryable, so an event can't be half-accepted and lost if delivery fails mid-way</li>
<li>Nested PowerShell commands no longer flood errors and look like a hang. Commands run through an outer PowerShell bootstrap, so a nested <code>powershell -Command "... $_ ..."</code> had <code>$_</code> interpolated away by the outer parser before the inner shell saw it — a <code>Where-Object { $_.Name ... }</code> pipeline then errored once per item over a large tree while still exiting 0. Redundant nested invocations are now unwrapped and run directly, only when that provably preserves semantics (same PowerShell edition, no profile loading, fully quoted command). Cline is also told which edition it's actually on — Windows PowerShell vs. Microsoft PowerShell — and to stop wrapping commands in a shell it's already running in</li>
<li>Prompt telemetry to Cline's tracing backend is now limited to Cline and Cline Pass requests. Turns run against your own provider keys are not traced</li>
<li>Cline Pass now appears in the composer's provider picker alongside Cline. One Cline sign-in configures both — Cline Pass stores its credentials under the Cline account rather than having its own — but the picker only listed providers with their own saved settings entry, and onboarding writes just the Cline one. A new Cline Pass user therefore saw a single row</li>
<li>The composer's provider picker has a <strong>Set up another provider</strong> row at the bottom that opens Settings → Models. The picker only lists what you have already configured, so there was no way to reach the rest of the catalog from the composer</li>
<li>Dropped the "Configured" checkmark from the composer's provider picker (added in 0.0.25). Every row in that picker has a saved settings entry, so for anyone who set their providers up in the app every row carried the same green check and it read as decoration. Settings is still where provider readiness is shown, and it distinguishes a real credential from an entry a legacy migration left behind</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.25...desktop-v0.0.26"><tt>desktop-v0.0.25...desktop-v0.0.26</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-11T07:46:46.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles臨時檔案託管：title.txt開放下載且將於59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/desktop-v0.0.25</link>
    <itunes:title><![CDATA[tmpfiles臨時檔案託管：title.txt開放下載且將於59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>ChatGPT Subscription (Codex) now lists only the models your plan can actually use. Two separate paths filled the picker from the shared OpenAI catalog, so GPT-4o, GPT-4.1, and <code>chatgpt-image-latest</code> showed up alongside the Codex models, and the runtime lost the Codex context caps. The model rules also match what the backend now accepts: <code>gpt-5.4</code> and <code>gpt-5.4-mini</code> were retired for ChatGPT accounts on 2026-08-31 and are gone, the default moves to <code>gpt-5.6-terra</code>, and every Codex model is capped at the real 400K/272K/128K backend budget instead of inheriting the API's 1.05M limits</li>
<li>Windows updates no longer fail with "Error opening file for writing". The compiled sidecar re-executes itself as the detached Cline Hub daemon, which outlives the app by design, and Tauri's NSIS installer only kills the main binary — so the daemon still held <code>code-sidecar.exe</code> and the install stopped until you killed the process by hand. The installer now stops it first, matched on the full path so updating one channel does not take down a side-by-side Cline Beta's sessions</li>
<li>Your prompt is no longer lost when a send fails before the turn starts — switching to Codex and having the OAuth refresh throw, for instance. The runtime never took the prompt, so post-send hydration wiped the optimistic bubble and you had to retype it. The text and attachments now come back to the composer, merged with anything you attached while the send was pending, and left alone if you have already started typing something else</li>
<li>Providers that authenticate through a local CLI — Claude Code, Codex CLI — can now start sessions without an API key. They showed as Configured in Settings via their local-auth capability, but session start still refused them with "Missing API key"</li>
<li>OpenCode is now treated as a local CLI provider rather than an OAuth one, so it shows the local CLI notice instead of a browser sign-in button that could not do anything. It authenticates from the credentials the opencode CLI itself stores</li>
<li>Session import from Claude Code, Codex, and opencode has its own page in Settings instead of a row buried in General</li>
<li>The composer's provider picker now marks which providers you have already configured</li>
<li>The model picker distinguishes models that share a name, and Cline Pass subscription models are listed separately from the free fallback tier</li>
<li>Published DMGs use the intended window layout and background again. Tauri skips the Finder AppleScript that applies them whenever <code>CI</code> is set, which GitHub Actions always sets, so every DMG since the artwork landed shipped with a stock Finder window even though the artwork was generated and validated</li>
<li>Cline's recommended, free, and subscribed model lists now ship with the app, so they are correct at first launch instead of waiting on a live catalog fetch</li>
<li>Refreshed the model catalog. Adds NaN (nan.builders) and changes the resolved default model for 36 providers — including Bedrock, Vertex, OpenRouter, Kilo, GitHub Copilot, Gemini, Cerebras, Fireworks, Requesty, and Vercel AI Gateway. Several move off Claude Fable 5.1 to GPT-6 Astra, Vertex goes to Gemini 3.8 Flash, and OpenRouter/Kilo to Inception Mercury 2.5. If you use one of those without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.24...desktop-v0.0.25"><tt>desktop-v0.0.24...desktop-v0.0.25</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>ChatGPT Subscription (Codex) now lists only the models your plan can actually use. Two separate paths filled the picker from the shared OpenAI catalog, so GPT-4o, GPT-4.1, and <code>chatgpt-image-latest</code> showed up alongside the Codex models, and the runtime lost the Codex context caps. The model rules also match what the backend now accepts: <code>gpt-5.4</code> and <code>gpt-5.4-mini</code> were retired for ChatGPT accounts on 2026-08-31 and are gone, the default moves to <code>gpt-5.6-terra</code>, and every Codex model is capped at the real 400K/272K/128K backend budget instead of inheriting the API's 1.05M limits</li>
<li>Windows updates no longer fail with "Error opening file for writing". The compiled sidecar re-executes itself as the detached Cline Hub daemon, which outlives the app by design, and Tauri's NSIS installer only kills the main binary — so the daemon still held <code>code-sidecar.exe</code> and the install stopped until you killed the process by hand. The installer now stops it first, matched on the full path so updating one channel does not take down a side-by-side Cline Beta's sessions</li>
<li>Your prompt is no longer lost when a send fails before the turn starts — switching to Codex and having the OAuth refresh throw, for instance. The runtime never took the prompt, so post-send hydration wiped the optimistic bubble and you had to retype it. The text and attachments now come back to the composer, merged with anything you attached while the send was pending, and left alone if you have already started typing something else</li>
<li>Providers that authenticate through a local CLI — Claude Code, Codex CLI — can now start sessions without an API key. They showed as Configured in Settings via their local-auth capability, but session start still refused them with "Missing API key"</li>
<li>OpenCode is now treated as a local CLI provider rather than an OAuth one, so it shows the local CLI notice instead of a browser sign-in button that could not do anything. It authenticates from the credentials the opencode CLI itself stores</li>
<li>Session import from Claude Code, Codex, and opencode has its own page in Settings instead of a row buried in General</li>
<li>The composer's provider picker now marks which providers you have already configured</li>
<li>The model picker distinguishes models that share a name, and Cline Pass subscription models are listed separately from the free fallback tier</li>
<li>Published DMGs use the intended window layout and background again. Tauri skips the Finder AppleScript that applies them whenever <code>CI</code> is set, which GitHub Actions always sets, so every DMG since the artwork landed shipped with a stock Finder window even though the artwork was generated and validated</li>
<li>Cline's recommended, free, and subscribed model lists now ship with the app, so they are correct at first launch instead of waiting on a live catalog fetch</li>
<li>Refreshed the model catalog. Adds NaN (nan.builders) and changes the resolved default model for 36 providers — including Bedrock, Vertex, OpenRouter, Kilo, GitHub Copilot, Gemini, Cerebras, Fireworks, Requesty, and Vercel AI Gateway. Several move off Claude Fable 5.1 to GPT-6 Astra, Vertex goes to Gemini 3.8 Flash, and OpenRouter/Kilo to Inception Mercury 2.5. If you use one of those without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.24...desktop-v0.0.25"><tt>desktop-v0.0.24...desktop-v0.0.25</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>ChatGPT Subscription (Codex) now lists only the models your plan can actually use. Two separate paths filled the picker from the shared OpenAI catalog, so GPT-4o, GPT-4.1, and <code>chatgpt-image-latest</code> showed up alongside the Codex models, and the runtime lost the Codex context caps. The model rules also match what the backend now accepts: <code>gpt-5.4</code> and <code>gpt-5.4-mini</code> were retired for ChatGPT accounts on 2026-08-31 and are gone, the default moves to <code>gpt-5.6-terra</code>, and every Codex model is capped at the real 400K/272K/128K backend budget instead of inheriting the API's 1.05M limits</li>
<li>Windows updates no longer fail with "Error opening file for writing". The compiled sidecar re-executes itself as the detached Cline Hub daemon, which outlives the app by design, and Tauri's NSIS installer only kills the main binary — so the daemon still held <code>code-sidecar.exe</code> and the install stopped until you killed the process by hand. The installer now stops it first, matched on the full path so updating one channel does not take down a side-by-side Cline Beta's sessions</li>
<li>Your prompt is no longer lost when a send fails before the turn starts — switching to Codex and having the OAuth refresh throw, for instance. The runtime never took the prompt, so post-send hydration wiped the optimistic bubble and you had to retype it. The text and attachments now come back to the composer, merged with anything you attached while the send was pending, and left alone if you have already started typing something else</li>
<li>Providers that authenticate through a local CLI — Claude Code, Codex CLI — can now start sessions without an API key. They showed as Configured in Settings via their local-auth capability, but session start still refused them with "Missing API key"</li>
<li>OpenCode is now treated as a local CLI provider rather than an OAuth one, so it shows the local CLI notice instead of a browser sign-in button that could not do anything. It authenticates from the credentials the opencode CLI itself stores</li>
<li>Session import from Claude Code, Codex, and opencode has its own page in Settings instead of a row buried in General</li>
<li>The composer's provider picker now marks which providers you have already configured</li>
<li>The model picker distinguishes models that share a name, and Cline Pass subscription models are listed separately from the free fallback tier</li>
<li>Published DMGs use the intended window layout and background again. Tauri skips the Finder AppleScript that applies them whenever <code>CI</code> is set, which GitHub Actions always sets, so every DMG since the artwork landed shipped with a stock Finder window even though the artwork was generated and validated</li>
<li>Cline's recommended, free, and subscribed model lists now ship with the app, so they are correct at first launch instead of waiting on a live catalog fetch</li>
<li>Refreshed the model catalog. Adds NaN (nan.builders) and changes the resolved default model for 36 providers — including Bedrock, Vertex, OpenRouter, Kilo, GitHub Copilot, Gemini, Cerebras, Fireworks, Requesty, and Vercel AI Gateway. Several move off Claude Fable 5.1 to GPT-6 Astra, Vertex goes to Gemini 3.8 Flash, and OpenRouter/Kilo to Inception Mercury 2.5. If you use one of those without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.24...desktop-v0.0.25"><tt>desktop-v0.0.24...desktop-v0.0.25</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-10T04:57:18.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles臨時託管平臺提供title.txt下載　檔案將在59分鐘後失效過期]]></title>
    <link>https://github.com/cline/cline/releases/tag/desktop-v0.0.23</link>
    <itunes:title><![CDATA[tmpfiles臨時託管平臺提供title.txt下載　檔案將在59分鐘後失效過期]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>Agent Plugins are now discovered and run by the shared Hub. Packages under <code>~/.agents/plugins</code> are validated from their <code>plugin.json</code>, their valid Agent Skills become available to the agent, and their stdio / Streamable HTTP / SSE MCP servers start automatically. Settings → Customize lists Agent Plugins separately from Cline Plugins, with each plugin's description, badge, and contributed tools, and enable/disable is Hub-managed per plugin. Workspace <code>.agents/plugins</code> directories are intentionally ignored</li>
<li>The "Cline Hub was updated" dialog no longer appears on every launch and reconnect. The app no longer prompts about a Hub running the same core version it does — a desktop and CLI release cut from different commits bundle the same core but never share a build fingerprint, so anyone with both installed got a dialog whose "Update and restart" looped on "no app update available". The build-mismatch dialog now also waits until an app update is actually staged, and "Later" sticks across session switches, reloads, and relaunches instead of resurfacing every time. A Hub the app genuinely cannot talk to still warns every time</li>
<li>Signing in now shows the device confirmation code in the app while you wait on the browser, so you can match it against the code the browser asks you to confirm — in onboarding, Account settings, and the provider list</li>
<li>Voice input failures caused by provider setup — missing credentials, transcription config — now take you straight to voice settings instead of a toast you cannot act on. Genuine microphone permission failures still toast, with a clearer message</li>
<li>Fixed the scheduled-task report vanishing when a finished run's step collapsed</li>
<li>Fixed one wedged MCP server blocking the rest from shutting down, leaking their processes</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.22...desktop-v0.0.23"><tt>desktop-v0.0.22...desktop-v0.0.23</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>Agent Plugins are now discovered and run by the shared Hub. Packages under <code>~/.agents/plugins</code> are validated from their <code>plugin.json</code>, their valid Agent Skills become available to the agent, and their stdio / Streamable HTTP / SSE MCP servers start automatically. Settings → Customize lists Agent Plugins separately from Cline Plugins, with each plugin's description, badge, and contributed tools, and enable/disable is Hub-managed per plugin. Workspace <code>.agents/plugins</code> directories are intentionally ignored</li>
<li>The "Cline Hub was updated" dialog no longer appears on every launch and reconnect. The app no longer prompts about a Hub running the same core version it does — a desktop and CLI release cut from different commits bundle the same core but never share a build fingerprint, so anyone with both installed got a dialog whose "Update and restart" looped on "no app update available". The build-mismatch dialog now also waits until an app update is actually staged, and "Later" sticks across session switches, reloads, and relaunches instead of resurfacing every time. A Hub the app genuinely cannot talk to still warns every time</li>
<li>Signing in now shows the device confirmation code in the app while you wait on the browser, so you can match it against the code the browser asks you to confirm — in onboarding, Account settings, and the provider list</li>
<li>Voice input failures caused by provider setup — missing credentials, transcription config — now take you straight to voice settings instead of a toast you cannot act on. Genuine microphone permission failures still toast, with a clearer message</li>
<li>Fixed the scheduled-task report vanishing when a finished run's step collapsed</li>
<li>Fixed one wedged MCP server blocking the rest from shutting down, leaking their processes</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.22...desktop-v0.0.23"><tt>desktop-v0.0.22...desktop-v0.0.23</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>Agent Plugins are now discovered and run by the shared Hub. Packages under <code>~/.agents/plugins</code> are validated from their <code>plugin.json</code>, their valid Agent Skills become available to the agent, and their stdio / Streamable HTTP / SSE MCP servers start automatically. Settings → Customize lists Agent Plugins separately from Cline Plugins, with each plugin's description, badge, and contributed tools, and enable/disable is Hub-managed per plugin. Workspace <code>.agents/plugins</code> directories are intentionally ignored</li>
<li>The "Cline Hub was updated" dialog no longer appears on every launch and reconnect. The app no longer prompts about a Hub running the same core version it does — a desktop and CLI release cut from different commits bundle the same core but never share a build fingerprint, so anyone with both installed got a dialog whose "Update and restart" looped on "no app update available". The build-mismatch dialog now also waits until an app update is actually staged, and "Later" sticks across session switches, reloads, and relaunches instead of resurfacing every time. A Hub the app genuinely cannot talk to still warns every time</li>
<li>Signing in now shows the device confirmation code in the app while you wait on the browser, so you can match it against the code the browser asks you to confirm — in onboarding, Account settings, and the provider list</li>
<li>Voice input failures caused by provider setup — missing credentials, transcription config — now take you straight to voice settings instead of a toast you cannot act on. Genuine microphone permission failures still toast, with a clearer message</li>
<li>Fixed the scheduled-task report vanishing when a finished run's step collapsed</li>
<li>Fixed one wedged MCP server blocking the rest from shutting down, leaking their processes</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.22...desktop-v0.0.23"><tt>desktop-v0.0.22...desktop-v0.0.23</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-03T18:33:16.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles暫存檔案分享：title.txt（4.21 KB）開放下載 59分鐘後過期]]></title>
    <link>https://github.com/cline/cline/releases/tag/v4.1.17</link>
    <itunes:title><![CDATA[tmpfiles暫存檔案分享：title.txt（4.21 KB）開放下載 59分鐘後過期]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>Everything here lands through the SDK bundle, so it applies to windows running that bundle.</p>
<h3>Added</h3>
<ul>
<li>ClinePass is now surfaced across the app: a card on the account page describing what the plan covers, a hint in provider settings, and a banner on the home screen. Dismissed banners stay dismissed.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed the background Hub process ballooning in memory during long sessions. Session status updates broadcast a full copy of the conversation transcript to every connected client, so on a large task each status change shipped megabytes and could grow the process to tens of gigabytes. Snapshots now carry state only.</li>
<li>Hook scripts that fail to spawn no longer crash the extension's core process and take the running task down with them.</li>
<li>Fixed a chat render crash on malformed <code>api_req</code> payloads.</li>
<li>Cost estimates no longer appear in task history for subscription-billed tasks (ClinePass, ChatGPT via Codex, and Claude Code), matching the task header.</li>
<li>Pasted provider API keys are now stripped of the invisible characters clipboards smuggle in (newlines, zero-width spaces, BOM). A key corrupted that way was hidden by the masked field and rejected by the provider with a 401 indistinguishable from a genuinely wrong key. Credential rejections now say that the API key is the problem and point at its configuration, keeping the provider's raw response as a diagnostic tail.</li>
<li>Signing in to OpenAI Codex (ChatGPT subscription) now fails with a clear "port in use" error when callback port 1455 is occupied. Previously the button opened a browser to a flow whose callback could never arrive, and nothing else happened. OAuth redirect errors such as <code>access_denied</code> are surfaced instead of being reported as a missing authorization code.</li>
<li>A transient network failure while refreshing OpenAI Codex or OpenAI-compatible-account tokens no longer signs you out. Only a genuinely rejected refresh token now requires re-authentication.</li>
<li>Fixed tool calling being silently disabled for Dify, SAP AI Core, opencode, and Codex CLI models. Their catalog entries declare no capabilities, and the empty list was read as an authoritative denial that stripped every tool from the request.</li>
<li>Fixed images being dropped from file reads on models whose capability list is empty.</li>
<li>Restoring a checkpoint now refuses to run when commits were made after it, instead of silently knocking them off the branch where only the reflog could recover them. Chat-only restore is unaffected.</li>
<li><code>apply_patch</code> now preserves a file's existing CRLF line endings.</li>
<li>Global rules are now also read from <code>~/Cline/Rules</code>, which is where the Rules tab writes them on WSL and headless installs whose Documents folder resolves to the home directory.</li>
<li>An enabled but unreachable remote (SSE or streamable HTTP) MCP server no longer stalls session startup; remote connects now have a 10 second budget.</li>
<li>Aborting a task now also cancels the delegated subagents and teammates it spawned, instead of leaving their work running.</li>
<li>Langfuse tracing now works in released builds. Detection identified the OpenTelemetry provider by class name, which minification renames, so tracing silently initialized as not ready in every published build while working in development.</li>
<li>Cline provider models are now read from the live catalog, so newly published models appear without an extension update.</li>
<li>Hook execution telemetry now fires; the task id was not threaded into hook runner creation, so those events were dropped.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Refreshed the built-in model catalog. Adds ten providers (Bothub, OpenReason, SenseNova (China), TokenGo, TokenRouter, Vancine, Volcengine Ark, Volcengine Ark Coding Plan, above.dev, and klokintegration.se) and updates model lists and pricing throughout. This is an unusually wide refresh: the resolved default model changes for 57 providers, most consequentially Anthropic, which now resolves to Claude Fable 5.1 instead of Claude Opus 5, with Amazon Bedrock, Vertex, OpenRouter, Vercel AI Gateway, Kilo Gateway, LLM Gateway, DevPass, DigitalOcean, CrossModel, Eden AI, and NanoGPT following. If you use a provider without pinning a model, expect a different default.</li>
<li>The message the model receives when you reject a tool call now names the rejected tool and reads as your decision rather than an error.</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/v4.1.16...v4.1.17"><tt>v4.1.16...v4.1.17</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>Everything here lands through the SDK bundle, so it applies to windows running that bundle.</p>
<h3>Added</h3>
<ul>
<li>ClinePass is now surfaced across the app: a card on the account page describing what the plan covers, a hint in provider settings, and a banner on the home screen. Dismissed banners stay dismissed.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed the background Hub process ballooning in memory during long sessions. Session status updates broadcast a full copy of the conversation transcript to every connected client, so on a large task each status change shipped megabytes and could grow the process to tens of gigabytes. Snapshots now carry state only.</li>
<li>Hook scripts that fail to spawn no longer crash the extension's core process and take the running task down with them.</li>
<li>Fixed a chat render crash on malformed <code>api_req</code> payloads.</li>
<li>Cost estimates no longer appear in task history for subscription-billed tasks (ClinePass, ChatGPT via Codex, and Claude Code), matching the task header.</li>
<li>Pasted provider API keys are now stripped of the invisible characters clipboards smuggle in (newlines, zero-width spaces, BOM). A key corrupted that way was hidden by the masked field and rejected by the provider with a 401 indistinguishable from a genuinely wrong key. Credential rejections now say that the API key is the problem and point at its configuration, keeping the provider's raw response as a diagnostic tail.</li>
<li>Signing in to OpenAI Codex (ChatGPT subscription) now fails with a clear "port in use" error when callback port 1455 is occupied. Previously the button opened a browser to a flow whose callback could never arrive, and nothing else happened. OAuth redirect errors such as <code>access_denied</code> are surfaced instead of being reported as a missing authorization code.</li>
<li>A transient network failure while refreshing OpenAI Codex or OpenAI-compatible-account tokens no longer signs you out. Only a genuinely rejected refresh token now requires re-authentication.</li>
<li>Fixed tool calling being silently disabled for Dify, SAP AI Core, opencode, and Codex CLI models. Their catalog entries declare no capabilities, and the empty list was read as an authoritative denial that stripped every tool from the request.</li>
<li>Fixed images being dropped from file reads on models whose capability list is empty.</li>
<li>Restoring a checkpoint now refuses to run when commits were made after it, instead of silently knocking them off the branch where only the reflog could recover them. Chat-only restore is unaffected.</li>
<li><code>apply_patch</code> now preserves a file's existing CRLF line endings.</li>
<li>Global rules are now also read from <code>~/Cline/Rules</code>, which is where the Rules tab writes them on WSL and headless installs whose Documents folder resolves to the home directory.</li>
<li>An enabled but unreachable remote (SSE or streamable HTTP) MCP server no longer stalls session startup; remote connects now have a 10 second budget.</li>
<li>Aborting a task now also cancels the delegated subagents and teammates it spawned, instead of leaving their work running.</li>
<li>Langfuse tracing now works in released builds. Detection identified the OpenTelemetry provider by class name, which minification renames, so tracing silently initialized as not ready in every published build while working in development.</li>
<li>Cline provider models are now read from the live catalog, so newly published models appear without an extension update.</li>
<li>Hook execution telemetry now fires; the task id was not threaded into hook runner creation, so those events were dropped.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Refreshed the built-in model catalog. Adds ten providers (Bothub, OpenReason, SenseNova (China), TokenGo, TokenRouter, Vancine, Volcengine Ark, Volcengine Ark Coding Plan, above.dev, and klokintegration.se) and updates model lists and pricing throughout. This is an unusually wide refresh: the resolved default model changes for 57 providers, most consequentially Anthropic, which now resolves to Claude Fable 5.1 instead of Claude Opus 5, with Amazon Bedrock, Vertex, OpenRouter, Vercel AI Gateway, Kilo Gateway, LLM Gateway, DevPass, DigitalOcean, CrossModel, Eden AI, and NanoGPT following. If you use a provider without pinning a model, expect a different default.</li>
<li>The message the model receives when you reject a tool call now names the rejected tool and reads as your decision rather than an error.</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/v4.1.16...v4.1.17"><tt>v4.1.16...v4.1.17</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<p>Everything here lands through the SDK bundle, so it applies to windows running that bundle.</p>
<h3>Added</h3>
<ul>
<li>ClinePass is now surfaced across the app: a card on the account page describing what the plan covers, a hint in provider settings, and a banner on the home screen. Dismissed banners stay dismissed.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Fixed the background Hub process ballooning in memory during long sessions. Session status updates broadcast a full copy of the conversation transcript to every connected client, so on a large task each status change shipped megabytes and could grow the process to tens of gigabytes. Snapshots now carry state only.</li>
<li>Hook scripts that fail to spawn no longer crash the extension's core process and take the running task down with them.</li>
<li>Fixed a chat render crash on malformed <code>api_req</code> payloads.</li>
<li>Cost estimates no longer appear in task history for subscription-billed tasks (ClinePass, ChatGPT via Codex, and Claude Code), matching the task header.</li>
<li>Pasted provider API keys are now stripped of the invisible characters clipboards smuggle in (newlines, zero-width spaces, BOM). A key corrupted that way was hidden by the masked field and rejected by the provider with a 401 indistinguishable from a genuinely wrong key. Credential rejections now say that the API key is the problem and point at its configuration, keeping the provider's raw response as a diagnostic tail.</li>
<li>Signing in to OpenAI Codex (ChatGPT subscription) now fails with a clear "port in use" error when callback port 1455 is occupied. Previously the button opened a browser to a flow whose callback could never arrive, and nothing else happened. OAuth redirect errors such as <code>access_denied</code> are surfaced instead of being reported as a missing authorization code.</li>
<li>A transient network failure while refreshing OpenAI Codex or OpenAI-compatible-account tokens no longer signs you out. Only a genuinely rejected refresh token now requires re-authentication.</li>
<li>Fixed tool calling being silently disabled for Dify, SAP AI Core, opencode, and Codex CLI models. Their catalog entries declare no capabilities, and the empty list was read as an authoritative denial that stripped every tool from the request.</li>
<li>Fixed images being dropped from file reads on models whose capability list is empty.</li>
<li>Restoring a checkpoint now refuses to run when commits were made after it, instead of silently knocking them off the branch where only the reflog could recover them. Chat-only restore is unaffected.</li>
<li><code>apply_patch</code> now preserves a file's existing CRLF line endings.</li>
<li>Global rules are now also read from <code>~/Cline/Rules</code>, which is where the Rules tab writes them on WSL and headless installs whose Documents folder resolves to the home directory.</li>
<li>An enabled but unreachable remote (SSE or streamable HTTP) MCP server no longer stalls session startup; remote connects now have a 10 second budget.</li>
<li>Aborting a task now also cancels the delegated subagents and teammates it spawned, instead of leaving their work running.</li>
<li>Langfuse tracing now works in released builds. Detection identified the OpenTelemetry provider by class name, which minification renames, so tracing silently initialized as not ready in every published build while working in development.</li>
<li>Cline provider models are now read from the live catalog, so newly published models appear without an extension update.</li>
<li>Hook execution telemetry now fires; the task id was not threaded into hook runner creation, so those events were dropped.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Refreshed the built-in model catalog. Adds ten providers (Bothub, OpenReason, SenseNova (China), TokenGo, TokenRouter, Vancine, Volcengine Ark, Volcengine Ark Coding Plan, above.dev, and klokintegration.se) and updates model lists and pricing throughout. This is an unusually wide refresh: the resolved default model changes for 57 providers, most consequentially Anthropic, which now resolves to Claude Fable 5.1 instead of Claude Opus 5, with Amazon Bedrock, Vertex, OpenRouter, Vercel AI Gateway, Kilo Gateway, LLM Gateway, DevPass, DigitalOcean, CrossModel, Eden AI, and NanoGPT following. If you use a provider without pinning a model, expect a different default.</li>
<li>The message the model receives when you reject a tool call now names the rejected tool and reads as your decision rather than an error.</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/v4.1.16...v4.1.17"><tt>v4.1.16...v4.1.17</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-02T05:40:07.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles 暫存檔案「title.txt」下載頁：剩餘 59 分鐘有效期限]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fshared%2Fv0.0.82</link>
    <itunes:title><![CDATA[tmpfiles 暫存檔案「title.txt」下載頁：剩餘 59 分鐘有效期限]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/shared@0.0.82</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/shared@0.0.82</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/shared@0.0.82</p>]]></content:encoded>
    
    <pubDate>2026-09-02T04:39:57.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles暫存檔案title.txt提供下載　大小6.35KB且將於59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fsdk%2Fv0.0.82</link>
    <itunes:title><![CDATA[tmpfiles暫存檔案title.txt提供下載　大小6.35KB且將於59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>Fixed tool calling being silently disabled for gateway models whose catalog entry declares no capabilities. Three separate producers built gateway model definitions with their own hand-written capability translations and had drifted; the builtin-provider path emitted <code>["text"]</code> where the others emitted <code>undefined</code>, and that list read as an authoritative denial that stripped every tool definition from requests to Dify, SAP AI Core, opencode, and the Codex CLI. One shared translator now serves every producer</li>
<li>Fixed an empty capability list stripping image input. Two readers bypassed <code>modelHasCapability</code> and tested <code>capabilities</code> directly, where an empty list is not nullish but matches nothing: the file-read tool silently dropped every image from the request, and model pickers reported models as definitively lacking vision, attachments, and reasoning when nothing had been declared</li>
<li>Langfuse tracing now works in minified release builds. <code>initializeLangfuseTelemetry</code> identified the OpenTelemetry provider by constructor name, which minification renames, so every production build silently initialized with <code>readiness=false</code> while unminified dev runs worked. Detection is now structural, and a foreign provider already holding the global slot gets the Langfuse span processor attached rather than being silently bailed on. Langfuse also moved to AI SDK 7 telemetry</li>
<li>New <code>SessionImportService</code> discovers and imports conversation history from Claude Code (<code>~/.claude/projects</code>), Codex (<code>~/.codex/sessions</code>), and opencode (<code>opencode.db</code>), translating each into Cline's native message format and persisting it as a completed, listable, resumable session. Imports are transactional, idempotent per source, and sanitized so the transcript is valid to resume against any provider</li>
<li>The hub is never retired out from under running work. <code>upgradeManagedHub</code> and <code>retireIncompatibleHub</code> now establish the drain barrier before any activity check, and refuse to replace a hub that does not accept the drain — previously an idle snapshot could authorize a shutdown that killed a session admitted moments later</li>
<li>Restoring a checkpoint now refuses to run when HEAD has moved past the commit the checkpoint was taken on. The restore's <code>git reset --hard</code> silently knocked any later commits off the branch, leaving them reachable only through the reflog; the branch move is now a compare-and-swap so a commit landing mid-restore cannot be lost either</li>
<li>Remote (SSE/streamable HTTP) MCP servers now get a 10s connect budget. An unreachable but enabled server previously spent the full 60s request timeout inside <code>session.create</code>, blowing past the hub's 30s cap and tearing down the whole session</li>
<li><code>apply_patch</code> now preserves a file's own CRLF line endings across updates</li>
<li>Global rules are now also discovered at <code>~/Cline/Rules</code>. The VS Code Rules tab resolves the Documents folder with <code>xdg-user-dir DOCUMENTS</code>, which prints bare <code>$HOME</code> on WSL and headless installs, so rules written there never reached the system prompt</li>
<li>OpenAI Codex (ChatGPT subscription) sign-in now fails fast with an actionable error when callback port 1455 is already in use, instead of opening a browser to a flow whose callback can never arrive. OAuth redirect errors such as <code>access_denied</code> are surfaced rather than collapsed into "Missing authorization code"</li>
<li>A transient token-refresh failure no longer logs out Codex and OpenAI-compatible-account users. Only a rejected refresh token now forces re-authentication; network errors, timeouts, and server 5xx rethrow instead</li>
<li>Aborting a session now propagates to the work it spawned — delegated subagents and teammates are cancelled with it, and aborted teammate tasks persist as cancelled instead of staying open</li>
<li>Agent-created schedules are now anchored in <code>~/.cline/schedules</code> rather than inheriting whichever chat workspace they happened to be created in, where they were invisible to workspace-scoped listings and tied to folders that may be cleaned up. Schedules created with an explicit workspace are unchanged</li>
<li>Fixed hub-managed schedules being wiped by cron reconciliation on hub restart</li>
<li>Scheduled sessions now carry their schedule's identity — id, name, cron run id, and a stable 1-based run number that a later cancellation never shifts — so clients can group runs under the schedule that produced them</li>
<li>Token-authenticated hub connections can now request cross-workspace schedule access, so a host can list schedules that live outside the workspace it registered. Workspace-bound clients stay scoped</li>
<li>Session history is now searchable through a server-side full-text index, exposed as a hub command, instead of clients loading every session to filter locally</li>
<li>Cline provider models now load from the live catalog rather than only the bundled one, so newly published models appear without a package update. Explicit runtime and <code>models.json</code> overrides still take precedence</li>
<li>The message the model receives when a user rejects a tool call now names the rejected tool and reads as a user decision rather than an error</li>
<li>Provider 401/403 responses are now classified as an <code>auth</code> error class on the existing <code>errorClass</code> plumbing, so hosts can render credential rejections as actionable guidance instead of a raw provider body</li>
<li>The provider catalog now exposes a computed <code>configured</code> flag, so a settings entry seeded with only a default model and no credentials no longer reads as a connected provider</li>
<li>Tool results can now carry media: images returned by a tool are extracted and exposed as attachments instead of raw base64 text</li>
<li>The hub no longer creates filesystem watchers on agenda spec directories while the todo tool is disabled, and <code>updateTask</code> reconciles external spec edits itself, so a spec edited on disk no longer fails every subsequent update with a signature mismatch</li>
<li>Refreshed the model catalog. Adds ten providers (Bothub, OpenReason, SenseNova (China), TokenGo, TokenRouter, Vancine, Volcengine Ark, Volcengine Ark Coding Plan, above.dev, and klokintegration.se) and updates model lists and pricing across providers. This is an unusually wide refresh: the resolved default model changes for 57 providers. Most consequentially, Anthropic now resolves to Claude Fable 5.1 instead of Claude Opus 5, and Amazon Bedrock, Vertex, OpenRouter, Vercel AI Gateway, Kilo Gateway, LLM Gateway, DevPass, DigitalOcean, CrossModel, Eden AI, and NanoGPT follow it to Fable 5.1. If you use any provider without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.81...sdk/sdk/v0.0.82"><tt>sdk/sdk/v0.0.81...sdk/sdk/v0.0.82</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>Fixed tool calling being silently disabled for gateway models whose catalog entry declares no capabilities. Three separate producers built gateway model definitions with their own hand-written capability translations and had drifted; the builtin-provider path emitted <code>["text"]</code> where the others emitted <code>undefined</code>, and that list read as an authoritative denial that stripped every tool definition from requests to Dify, SAP AI Core, opencode, and the Codex CLI. One shared translator now serves every producer</li>
<li>Fixed an empty capability list stripping image input. Two readers bypassed <code>modelHasCapability</code> and tested <code>capabilities</code> directly, where an empty list is not nullish but matches nothing: the file-read tool silently dropped every image from the request, and model pickers reported models as definitively lacking vision, attachments, and reasoning when nothing had been declared</li>
<li>Langfuse tracing now works in minified release builds. <code>initializeLangfuseTelemetry</code> identified the OpenTelemetry provider by constructor name, which minification renames, so every production build silently initialized with <code>readiness=false</code> while unminified dev runs worked. Detection is now structural, and a foreign provider already holding the global slot gets the Langfuse span processor attached rather than being silently bailed on. Langfuse also moved to AI SDK 7 telemetry</li>
<li>New <code>SessionImportService</code> discovers and imports conversation history from Claude Code (<code>~/.claude/projects</code>), Codex (<code>~/.codex/sessions</code>), and opencode (<code>opencode.db</code>), translating each into Cline's native message format and persisting it as a completed, listable, resumable session. Imports are transactional, idempotent per source, and sanitized so the transcript is valid to resume against any provider</li>
<li>The hub is never retired out from under running work. <code>upgradeManagedHub</code> and <code>retireIncompatibleHub</code> now establish the drain barrier before any activity check, and refuse to replace a hub that does not accept the drain — previously an idle snapshot could authorize a shutdown that killed a session admitted moments later</li>
<li>Restoring a checkpoint now refuses to run when HEAD has moved past the commit the checkpoint was taken on. The restore's <code>git reset --hard</code> silently knocked any later commits off the branch, leaving them reachable only through the reflog; the branch move is now a compare-and-swap so a commit landing mid-restore cannot be lost either</li>
<li>Remote (SSE/streamable HTTP) MCP servers now get a 10s connect budget. An unreachable but enabled server previously spent the full 60s request timeout inside <code>session.create</code>, blowing past the hub's 30s cap and tearing down the whole session</li>
<li><code>apply_patch</code> now preserves a file's own CRLF line endings across updates</li>
<li>Global rules are now also discovered at <code>~/Cline/Rules</code>. The VS Code Rules tab resolves the Documents folder with <code>xdg-user-dir DOCUMENTS</code>, which prints bare <code>$HOME</code> on WSL and headless installs, so rules written there never reached the system prompt</li>
<li>OpenAI Codex (ChatGPT subscription) sign-in now fails fast with an actionable error when callback port 1455 is already in use, instead of opening a browser to a flow whose callback can never arrive. OAuth redirect errors such as <code>access_denied</code> are surfaced rather than collapsed into "Missing authorization code"</li>
<li>A transient token-refresh failure no longer logs out Codex and OpenAI-compatible-account users. Only a rejected refresh token now forces re-authentication; network errors, timeouts, and server 5xx rethrow instead</li>
<li>Aborting a session now propagates to the work it spawned — delegated subagents and teammates are cancelled with it, and aborted teammate tasks persist as cancelled instead of staying open</li>
<li>Agent-created schedules are now anchored in <code>~/.cline/schedules</code> rather than inheriting whichever chat workspace they happened to be created in, where they were invisible to workspace-scoped listings and tied to folders that may be cleaned up. Schedules created with an explicit workspace are unchanged</li>
<li>Fixed hub-managed schedules being wiped by cron reconciliation on hub restart</li>
<li>Scheduled sessions now carry their schedule's identity — id, name, cron run id, and a stable 1-based run number that a later cancellation never shifts — so clients can group runs under the schedule that produced them</li>
<li>Token-authenticated hub connections can now request cross-workspace schedule access, so a host can list schedules that live outside the workspace it registered. Workspace-bound clients stay scoped</li>
<li>Session history is now searchable through a server-side full-text index, exposed as a hub command, instead of clients loading every session to filter locally</li>
<li>Cline provider models now load from the live catalog rather than only the bundled one, so newly published models appear without a package update. Explicit runtime and <code>models.json</code> overrides still take precedence</li>
<li>The message the model receives when a user rejects a tool call now names the rejected tool and reads as a user decision rather than an error</li>
<li>Provider 401/403 responses are now classified as an <code>auth</code> error class on the existing <code>errorClass</code> plumbing, so hosts can render credential rejections as actionable guidance instead of a raw provider body</li>
<li>The provider catalog now exposes a computed <code>configured</code> flag, so a settings entry seeded with only a default model and no credentials no longer reads as a connected provider</li>
<li>Tool results can now carry media: images returned by a tool are extracted and exposed as attachments instead of raw base64 text</li>
<li>The hub no longer creates filesystem watchers on agenda spec directories while the todo tool is disabled, and <code>updateTask</code> reconciles external spec edits itself, so a spec edited on disk no longer fails every subsequent update with a signature mismatch</li>
<li>Refreshed the model catalog. Adds ten providers (Bothub, OpenReason, SenseNova (China), TokenGo, TokenRouter, Vancine, Volcengine Ark, Volcengine Ark Coding Plan, above.dev, and klokintegration.se) and updates model lists and pricing across providers. This is an unusually wide refresh: the resolved default model changes for 57 providers. Most consequentially, Anthropic now resolves to Claude Fable 5.1 instead of Claude Opus 5, and Amazon Bedrock, Vertex, OpenRouter, Vercel AI Gateway, Kilo Gateway, LLM Gateway, DevPass, DigitalOcean, CrossModel, Eden AI, and NanoGPT follow it to Fable 5.1. If you use any provider without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.81...sdk/sdk/v0.0.82"><tt>sdk/sdk/v0.0.81...sdk/sdk/v0.0.82</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>Fixed tool calling being silently disabled for gateway models whose catalog entry declares no capabilities. Three separate producers built gateway model definitions with their own hand-written capability translations and had drifted; the builtin-provider path emitted <code>["text"]</code> where the others emitted <code>undefined</code>, and that list read as an authoritative denial that stripped every tool definition from requests to Dify, SAP AI Core, opencode, and the Codex CLI. One shared translator now serves every producer</li>
<li>Fixed an empty capability list stripping image input. Two readers bypassed <code>modelHasCapability</code> and tested <code>capabilities</code> directly, where an empty list is not nullish but matches nothing: the file-read tool silently dropped every image from the request, and model pickers reported models as definitively lacking vision, attachments, and reasoning when nothing had been declared</li>
<li>Langfuse tracing now works in minified release builds. <code>initializeLangfuseTelemetry</code> identified the OpenTelemetry provider by constructor name, which minification renames, so every production build silently initialized with <code>readiness=false</code> while unminified dev runs worked. Detection is now structural, and a foreign provider already holding the global slot gets the Langfuse span processor attached rather than being silently bailed on. Langfuse also moved to AI SDK 7 telemetry</li>
<li>New <code>SessionImportService</code> discovers and imports conversation history from Claude Code (<code>~/.claude/projects</code>), Codex (<code>~/.codex/sessions</code>), and opencode (<code>opencode.db</code>), translating each into Cline's native message format and persisting it as a completed, listable, resumable session. Imports are transactional, idempotent per source, and sanitized so the transcript is valid to resume against any provider</li>
<li>The hub is never retired out from under running work. <code>upgradeManagedHub</code> and <code>retireIncompatibleHub</code> now establish the drain barrier before any activity check, and refuse to replace a hub that does not accept the drain — previously an idle snapshot could authorize a shutdown that killed a session admitted moments later</li>
<li>Restoring a checkpoint now refuses to run when HEAD has moved past the commit the checkpoint was taken on. The restore's <code>git reset --hard</code> silently knocked any later commits off the branch, leaving them reachable only through the reflog; the branch move is now a compare-and-swap so a commit landing mid-restore cannot be lost either</li>
<li>Remote (SSE/streamable HTTP) MCP servers now get a 10s connect budget. An unreachable but enabled server previously spent the full 60s request timeout inside <code>session.create</code>, blowing past the hub's 30s cap and tearing down the whole session</li>
<li><code>apply_patch</code> now preserves a file's own CRLF line endings across updates</li>
<li>Global rules are now also discovered at <code>~/Cline/Rules</code>. The VS Code Rules tab resolves the Documents folder with <code>xdg-user-dir DOCUMENTS</code>, which prints bare <code>$HOME</code> on WSL and headless installs, so rules written there never reached the system prompt</li>
<li>OpenAI Codex (ChatGPT subscription) sign-in now fails fast with an actionable error when callback port 1455 is already in use, instead of opening a browser to a flow whose callback can never arrive. OAuth redirect errors such as <code>access_denied</code> are surfaced rather than collapsed into "Missing authorization code"</li>
<li>A transient token-refresh failure no longer logs out Codex and OpenAI-compatible-account users. Only a rejected refresh token now forces re-authentication; network errors, timeouts, and server 5xx rethrow instead</li>
<li>Aborting a session now propagates to the work it spawned — delegated subagents and teammates are cancelled with it, and aborted teammate tasks persist as cancelled instead of staying open</li>
<li>Agent-created schedules are now anchored in <code>~/.cline/schedules</code> rather than inheriting whichever chat workspace they happened to be created in, where they were invisible to workspace-scoped listings and tied to folders that may be cleaned up. Schedules created with an explicit workspace are unchanged</li>
<li>Fixed hub-managed schedules being wiped by cron reconciliation on hub restart</li>
<li>Scheduled sessions now carry their schedule's identity — id, name, cron run id, and a stable 1-based run number that a later cancellation never shifts — so clients can group runs under the schedule that produced them</li>
<li>Token-authenticated hub connections can now request cross-workspace schedule access, so a host can list schedules that live outside the workspace it registered. Workspace-bound clients stay scoped</li>
<li>Session history is now searchable through a server-side full-text index, exposed as a hub command, instead of clients loading every session to filter locally</li>
<li>Cline provider models now load from the live catalog rather than only the bundled one, so newly published models appear without a package update. Explicit runtime and <code>models.json</code> overrides still take precedence</li>
<li>The message the model receives when a user rejects a tool call now names the rejected tool and reads as a user decision rather than an error</li>
<li>Provider 401/403 responses are now classified as an <code>auth</code> error class on the existing <code>errorClass</code> plumbing, so hosts can render credential rejections as actionable guidance instead of a raw provider body</li>
<li>The provider catalog now exposes a computed <code>configured</code> flag, so a settings entry seeded with only a default model and no credentials no longer reads as a connected provider</li>
<li>Tool results can now carry media: images returned by a tool are extracted and exposed as attachments instead of raw base64 text</li>
<li>The hub no longer creates filesystem watchers on agenda spec directories while the todo tool is disabled, and <code>updateTask</code> reconciles external spec edits itself, so a spec edited on disk no longer fails every subsequent update with a signature mismatch</li>
<li>Refreshed the model catalog. Adds ten providers (Bothub, OpenReason, SenseNova (China), TokenGo, TokenRouter, Vancine, Volcengine Ark, Volcengine Ark Coding Plan, above.dev, and klokintegration.se) and updates model lists and pricing across providers. This is an unusually wide refresh: the resolved default model changes for 57 providers. Most consequentially, Anthropic now resolves to Claude Fable 5.1 instead of Claude Opus 5, and Amazon Bedrock, Vertex, OpenRouter, Vercel AI Gateway, Kilo Gateway, LLM Gateway, DevPass, DigitalOcean, CrossModel, Eden AI, and NanoGPT follow it to Fable 5.1. If you use any provider without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.81...sdk/sdk/v0.0.82"><tt>sdk/sdk/v0.0.81...sdk/sdk/v0.0.82</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-02T04:40:16.000Z</pubDate>
  </item><item>
    <title><![CDATA[臨時檔案託管網站 /tmp/files 之「title.txt」下載頁面，檔案將於 59 分鐘後過期]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fllms%2Fv0.0.82</link>
    <itunes:title><![CDATA[臨時檔案託管網站 /tmp/files 之「title.txt」下載頁面，檔案將於 59 分鐘後過期]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/llms@0.0.82</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/llms@0.0.82</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/llms@0.0.82</p>]]></content:encoded>
    
    <pubDate>2026-09-02T04:39:58.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles暫存檔案託管平台：title.txt開放下載 59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fcore%2Fv0.0.82</link>
    <itunes:title><![CDATA[tmpfiles暫存檔案託管平台：title.txt開放下載 59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/core@0.0.82</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/core@0.0.82</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/core@0.0.82</p>]]></content:encoded>
    
    <pubDate>2026-09-02T04:40:02.000Z</pubDate>
  </item></channel>
</rss>