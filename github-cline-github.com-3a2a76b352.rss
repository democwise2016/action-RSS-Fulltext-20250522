<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[cline Release [GH][RSS+]]]></title>
    <link>https://github.com/cline/cline/releases.atom</link>
    <image>
      <url>https://fastly.picsum.photos/id/530/800/800.jpg?hmac=4Y0guzyql8hlsm7b7O9KKEa9XPG0I9uqcZznpVa4t0Y</url>
      <title>cline Release [GH][RSS+]</title>
      <link>https://github.com/cline/cline/releases.atom</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://github.com/cline/cline/releases.atom" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[cline Release [GH][RSS+]]]></copyright>
    <itunes:author><![CDATA[cline Release [GH][RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      <a href="https://github.com/cline/cline/releases" target="_blank">https://github.com/cline/cline/releases</a><br />
<br />
<a href="https://github.com/cline/cline/releases.atom" target="_blank">https://github.com/cline/cline/releases.atom</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      <a href="https://github.com/cline/cline/releases" target="_blank">https://github.com/cline/cline/releases</a><br />
<br />
<a href="https://github.com/cline/cline/releases.atom" target="_blank">https://github.com/cline/cline/releases.atom</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[cline Release [GH][RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://fastly.picsum.photos/id/530/800/800.jpg?hmac=4Y0guzyql8hlsm7b7O9KKEa9XPG0I9uqcZznpVa4t0Y"/>
<item>
    <title><![CDATA[tmpfiles 臨時檔案分享：title.txt 開放下載，將於 59 分鐘後過期失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/desktop-v0.0.32</link>
    <itunes:title><![CDATA[tmpfiles 臨時檔案分享：title.txt 開放下載，將於 59 分鐘後過期失效]]></itunes:title>
    <itunes:author><![CDATA[saoudrizwan]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>Desktop v0.0.32</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>Desktop v0.0.32</p>]]>
    </description>
    <content:encoded><![CDATA[<p>Desktop v0.0.32</p>]]></content:encoded>
    
    <pubDate>2026-09-18T01:15:43.000Z</pubDate>
  </item><item>
    <title><![CDATA[/tmp/files臨時檔案託管頁面：title.txt開放下載且將於59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/v4.1.19</link>
    <itunes:title><![CDATA[/tmp/files臨時檔案託管頁面：title.txt開放下載且將於59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<h3>Added</h3>
<ul>
<li>Images attached to a model that cannot read them are now flagged instead of silently discarded. Thumbnails get a warning badge and the composer explains that the images will be ignored, with a button to switch to an image-capable model. Previously the thumbnail looked normal and the image was replaced with a text placeholder just before the request, so there was no way to tell it had been dropped. Model info and the attachment picker also report image support accurately for models that declare text-only input without listing capabilities.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Cline's logo and icons have been refreshed throughout the extension, including the activity bar and panel icons.</li>
<li>The W&amp;B Inference provider is now listed as CoreWeave, with its sign-up and API-key links updated to match CoreWeave's current documentation.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Long tasks now compact when they actually need to rather than running out of room. The trigger compared a character-based estimate of the conversation (~3 characters per token) against the model's context limit, so content that tokenizes far denser than that — disassembly, image dumps, minified sources — could fill the real context window while the estimate stayed under the threshold and compaction never fired; the turn was then squeezed down to a handful of output tokens. The trigger now also uses the token count the provider itself reports, and how much history is kept is scaled by how far off the estimate turned out to be. The summarizer's output budget also doubled, so a model that reasons before answering can no longer spend the whole budget thinking and return no summary at all.</li>
<li>On Windows, opening a repository that contains a file named <code>rg.exe</code>, <code>git.exe</code>, or <code>powershell.exe</code> no longer runs that file in place of the real program. Bare program names were resolved through the workspace directory before PATH, so a planted executable ran with your privileges as soon as the workspace was indexed. Cline now sets Windows' <code>NoDefaultCurrentDirectoryInExePath</code> opt-out at startup, in both the VS Code extension and the JetBrains core. Processes Cline launches inherit it, so inside a Command Prompt shell a program in the current directory now needs <code>.\</code> as it already does in PowerShell.</li>
<li>A model turn that fails mid-stream with a transient provider error is now retried up to three times with backoff instead of ending the task. A single rate-limit response forwarded by a gateway previously surfaced as a failed task. A turn that has already streamed output is never retried, so nothing is duplicated.</li>
<li>Terminal commands that succeed without printing anything (<code>git add -A</code> on a clean tree, for example) are now reported as empty output. They were treated as a shell-integration failure, which fed the model a snapshot of unrelated terminal scrollback prefixed with a warning that the output could not be captured, so silent commands intermittently looked like failures.</li>
<li>Checkpoints no longer re-hash every untracked file on each message. In workspaces holding large untracked directories this delayed every message by seconds to minutes; a persistent per-task index now lets git skip files it has already seen.</li>
<li><code>run_commands</code> no longer hangs until its timeout after a command that backgrounds a child process. The command had finished, but the backgrounded process held the output pipes open.</li>
<li><code>apply_patch</code> no longer silently overwrites an existing file when the model uses "Add File" on a path that already exists. The file's contents were replaced with no error and no record of what was lost.</li>
<li>PowerShell commands the model wrapped in another <code>powershell -Command "..."</code> are no longer parsed twice. The outer shell consumed <code>$_</code> before the inner command ran, so pipelines using it emitted an error for every item processed while still reporting success.</li>
<li>Opening your home directory as a workspace no longer drives the extension host to exhaustion. Typing an <code>@</code> mention indexed every file beneath it and re-ranked the whole index on each keystroke.</li>
<li>The "Supports Images" checkbox in OpenAI Compatible model settings now stays where you put it. The checkbox rendered the last committed value, and the re-sync that arrived during the save round-trip was re-emitted as a change event that wrote the old value back over your edit.</li>
<li>The error shown to the model when an <code>editor</code> call omits the text to replace now names the file and explains how to recover. The previous message was terse enough that some models re-sent the identical call until the task stopped.</li>
<li>Credentials are now stripped of invisible characters when saved, not only when pasted, and the cleanup covers AWS, GCP, and SAP fields and custom header values in addition to API keys.</li>
<li>Cline Pass now defaults to a model from your subscription rather than a free one. Its model list contains both tiers and the default was whichever model was published most recently, so a subscriber who never picked a model could be left on the free tier.</li>
<li>Cline Pass and free models now show no cost rather than the underlying market price, which is not what you are billed for.</li>
<li>Model pickers now fall back to the full Recommended, Free, and Subscribed lists when the models endpoint cannot be reached. The offline fallback was a short hardcoded list with no subscription tier at all.</li>
<li>Model lists for providers sharing the built-in catalog now refresh from the live catalog, so newly published models appear without an extension update, with timeouts so an unresponsive provider endpoint cannot stall the list.</li>
<li>Claude Code and OpenCode no longer ask for an API key they never read. Both authenticate from their own local CLI's credentials, but they were treated as key-based providers; the workaround was storing a dummy key. A missing CLI on <code>PATH</code> now warns rather than blocking, since a configured path or bundled binary also works.</li>
<li>The OpenAI Codex (ChatGPT subscription) model list no longer offers models the backend rejects, and Codex context limits are applied to every Codex model instead of being inherited from the OpenAI API catalog, which inflated both the context budget and the usage figures derived from it.</li>
<li>Models served by OpenCode Go are now sent over the wire protocol each one actually speaks. Every model was sent over the OpenAI chat-completions adapter, so models on that endpoint speaking other protocols failed or misbehaved.</li>
<li>Task history no longer goes blank when a task spawns many subagents. Subagent rows crowded out the tasks that created them, hiding the parent task and everything older.</li>
<li>Langfuse tracing, when configured, is now limited to the Cline and Cline Pass providers. The provider was ignored, so prompts and responses sent to third-party and bring-your-own-key providers were exported too.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Web search is now enabled by default on models that support it, outside YOLO mode. It can still be turned off in settings, and a settings file that cannot be read leaves it disabled rather than silently on.</li>
<li>The <code>run_commands</code> tool description now names the PowerShell edition in use — <code>Windows PowerShell (powershell.exe)</code> versus <code>PowerShell (pwsh.exe)</code> — quotes its guidance against that executable, and tells the model to run commands directly rather than wrapping them in another shell invocation. It also no longer describes the environment as Windows when <code>pwsh</code> is the configured shell on macOS or Linux.</li>
<li>Refreshed the built-in model catalog. Adds four providers (Infer by Flow7, Melious, NaN, and Wallaby) and takes the catalog from 5,788 to 6,079 models. This is a wide refresh: the resolved default model changes for 44 providers, most of them landing on DeepSeek V4.1 Flash — among them Hugging Face, Fireworks, Requesty, Nebius, Cortecs, CrossModel, DigitalOcean, Eden AI, and OpenCode Go. Gemini and Vertex now resolve to Gemini 3.8 Flash, GitHub Copilot and Vivgrid to GPT-6 Astra, and NVIDIA to GLM 5.3 Flash. If you use a provider without pinning a model, expect a different default.</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/v4.1.18...v4.1.19"><tt>v4.1.18...v4.1.19</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<h3>Added</h3>
<ul>
<li>Images attached to a model that cannot read them are now flagged instead of silently discarded. Thumbnails get a warning badge and the composer explains that the images will be ignored, with a button to switch to an image-capable model. Previously the thumbnail looked normal and the image was replaced with a text placeholder just before the request, so there was no way to tell it had been dropped. Model info and the attachment picker also report image support accurately for models that declare text-only input without listing capabilities.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Cline's logo and icons have been refreshed throughout the extension, including the activity bar and panel icons.</li>
<li>The W&amp;B Inference provider is now listed as CoreWeave, with its sign-up and API-key links updated to match CoreWeave's current documentation.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Long tasks now compact when they actually need to rather than running out of room. The trigger compared a character-based estimate of the conversation (~3 characters per token) against the model's context limit, so content that tokenizes far denser than that — disassembly, image dumps, minified sources — could fill the real context window while the estimate stayed under the threshold and compaction never fired; the turn was then squeezed down to a handful of output tokens. The trigger now also uses the token count the provider itself reports, and how much history is kept is scaled by how far off the estimate turned out to be. The summarizer's output budget also doubled, so a model that reasons before answering can no longer spend the whole budget thinking and return no summary at all.</li>
<li>On Windows, opening a repository that contains a file named <code>rg.exe</code>, <code>git.exe</code>, or <code>powershell.exe</code> no longer runs that file in place of the real program. Bare program names were resolved through the workspace directory before PATH, so a planted executable ran with your privileges as soon as the workspace was indexed. Cline now sets Windows' <code>NoDefaultCurrentDirectoryInExePath</code> opt-out at startup, in both the VS Code extension and the JetBrains core. Processes Cline launches inherit it, so inside a Command Prompt shell a program in the current directory now needs <code>.\</code> as it already does in PowerShell.</li>
<li>A model turn that fails mid-stream with a transient provider error is now retried up to three times with backoff instead of ending the task. A single rate-limit response forwarded by a gateway previously surfaced as a failed task. A turn that has already streamed output is never retried, so nothing is duplicated.</li>
<li>Terminal commands that succeed without printing anything (<code>git add -A</code> on a clean tree, for example) are now reported as empty output. They were treated as a shell-integration failure, which fed the model a snapshot of unrelated terminal scrollback prefixed with a warning that the output could not be captured, so silent commands intermittently looked like failures.</li>
<li>Checkpoints no longer re-hash every untracked file on each message. In workspaces holding large untracked directories this delayed every message by seconds to minutes; a persistent per-task index now lets git skip files it has already seen.</li>
<li><code>run_commands</code> no longer hangs until its timeout after a command that backgrounds a child process. The command had finished, but the backgrounded process held the output pipes open.</li>
<li><code>apply_patch</code> no longer silently overwrites an existing file when the model uses "Add File" on a path that already exists. The file's contents were replaced with no error and no record of what was lost.</li>
<li>PowerShell commands the model wrapped in another <code>powershell -Command "..."</code> are no longer parsed twice. The outer shell consumed <code>$_</code> before the inner command ran, so pipelines using it emitted an error for every item processed while still reporting success.</li>
<li>Opening your home directory as a workspace no longer drives the extension host to exhaustion. Typing an <code>@</code> mention indexed every file beneath it and re-ranked the whole index on each keystroke.</li>
<li>The "Supports Images" checkbox in OpenAI Compatible model settings now stays where you put it. The checkbox rendered the last committed value, and the re-sync that arrived during the save round-trip was re-emitted as a change event that wrote the old value back over your edit.</li>
<li>The error shown to the model when an <code>editor</code> call omits the text to replace now names the file and explains how to recover. The previous message was terse enough that some models re-sent the identical call until the task stopped.</li>
<li>Credentials are now stripped of invisible characters when saved, not only when pasted, and the cleanup covers AWS, GCP, and SAP fields and custom header values in addition to API keys.</li>
<li>Cline Pass now defaults to a model from your subscription rather than a free one. Its model list contains both tiers and the default was whichever model was published most recently, so a subscriber who never picked a model could be left on the free tier.</li>
<li>Cline Pass and free models now show no cost rather than the underlying market price, which is not what you are billed for.</li>
<li>Model pickers now fall back to the full Recommended, Free, and Subscribed lists when the models endpoint cannot be reached. The offline fallback was a short hardcoded list with no subscription tier at all.</li>
<li>Model lists for providers sharing the built-in catalog now refresh from the live catalog, so newly published models appear without an extension update, with timeouts so an unresponsive provider endpoint cannot stall the list.</li>
<li>Claude Code and OpenCode no longer ask for an API key they never read. Both authenticate from their own local CLI's credentials, but they were treated as key-based providers; the workaround was storing a dummy key. A missing CLI on <code>PATH</code> now warns rather than blocking, since a configured path or bundled binary also works.</li>
<li>The OpenAI Codex (ChatGPT subscription) model list no longer offers models the backend rejects, and Codex context limits are applied to every Codex model instead of being inherited from the OpenAI API catalog, which inflated both the context budget and the usage figures derived from it.</li>
<li>Models served by OpenCode Go are now sent over the wire protocol each one actually speaks. Every model was sent over the OpenAI chat-completions adapter, so models on that endpoint speaking other protocols failed or misbehaved.</li>
<li>Task history no longer goes blank when a task spawns many subagents. Subagent rows crowded out the tasks that created them, hiding the parent task and everything older.</li>
<li>Langfuse tracing, when configured, is now limited to the Cline and Cline Pass providers. The provider was ignored, so prompts and responses sent to third-party and bring-your-own-key providers were exported too.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Web search is now enabled by default on models that support it, outside YOLO mode. It can still be turned off in settings, and a settings file that cannot be read leaves it disabled rather than silently on.</li>
<li>The <code>run_commands</code> tool description now names the PowerShell edition in use — <code>Windows PowerShell (powershell.exe)</code> versus <code>PowerShell (pwsh.exe)</code> — quotes its guidance against that executable, and tells the model to run commands directly rather than wrapping them in another shell invocation. It also no longer describes the environment as Windows when <code>pwsh</code> is the configured shell on macOS or Linux.</li>
<li>Refreshed the built-in model catalog. Adds four providers (Infer by Flow7, Melious, NaN, and Wallaby) and takes the catalog from 5,788 to 6,079 models. This is a wide refresh: the resolved default model changes for 44 providers, most of them landing on DeepSeek V4.1 Flash — among them Hugging Face, Fireworks, Requesty, Nebius, Cortecs, CrossModel, DigitalOcean, Eden AI, and OpenCode Go. Gemini and Vertex now resolve to Gemini 3.8 Flash, GitHub Copilot and Vivgrid to GPT-6 Astra, and NVIDIA to GLM 5.3 Flash. If you use a provider without pinning a model, expect a different default.</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/v4.1.18...v4.1.19"><tt>v4.1.18...v4.1.19</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<h3>Added</h3>
<ul>
<li>Images attached to a model that cannot read them are now flagged instead of silently discarded. Thumbnails get a warning badge and the composer explains that the images will be ignored, with a button to switch to an image-capable model. Previously the thumbnail looked normal and the image was replaced with a text placeholder just before the request, so there was no way to tell it had been dropped. Model info and the attachment picker also report image support accurately for models that declare text-only input without listing capabilities.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Cline's logo and icons have been refreshed throughout the extension, including the activity bar and panel icons.</li>
<li>The W&amp;B Inference provider is now listed as CoreWeave, with its sign-up and API-key links updated to match CoreWeave's current documentation.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Long tasks now compact when they actually need to rather than running out of room. The trigger compared a character-based estimate of the conversation (~3 characters per token) against the model's context limit, so content that tokenizes far denser than that — disassembly, image dumps, minified sources — could fill the real context window while the estimate stayed under the threshold and compaction never fired; the turn was then squeezed down to a handful of output tokens. The trigger now also uses the token count the provider itself reports, and how much history is kept is scaled by how far off the estimate turned out to be. The summarizer's output budget also doubled, so a model that reasons before answering can no longer spend the whole budget thinking and return no summary at all.</li>
<li>On Windows, opening a repository that contains a file named <code>rg.exe</code>, <code>git.exe</code>, or <code>powershell.exe</code> no longer runs that file in place of the real program. Bare program names were resolved through the workspace directory before PATH, so a planted executable ran with your privileges as soon as the workspace was indexed. Cline now sets Windows' <code>NoDefaultCurrentDirectoryInExePath</code> opt-out at startup, in both the VS Code extension and the JetBrains core. Processes Cline launches inherit it, so inside a Command Prompt shell a program in the current directory now needs <code>.\</code> as it already does in PowerShell.</li>
<li>A model turn that fails mid-stream with a transient provider error is now retried up to three times with backoff instead of ending the task. A single rate-limit response forwarded by a gateway previously surfaced as a failed task. A turn that has already streamed output is never retried, so nothing is duplicated.</li>
<li>Terminal commands that succeed without printing anything (<code>git add -A</code> on a clean tree, for example) are now reported as empty output. They were treated as a shell-integration failure, which fed the model a snapshot of unrelated terminal scrollback prefixed with a warning that the output could not be captured, so silent commands intermittently looked like failures.</li>
<li>Checkpoints no longer re-hash every untracked file on each message. In workspaces holding large untracked directories this delayed every message by seconds to minutes; a persistent per-task index now lets git skip files it has already seen.</li>
<li><code>run_commands</code> no longer hangs until its timeout after a command that backgrounds a child process. The command had finished, but the backgrounded process held the output pipes open.</li>
<li><code>apply_patch</code> no longer silently overwrites an existing file when the model uses "Add File" on a path that already exists. The file's contents were replaced with no error and no record of what was lost.</li>
<li>PowerShell commands the model wrapped in another <code>powershell -Command "..."</code> are no longer parsed twice. The outer shell consumed <code>$_</code> before the inner command ran, so pipelines using it emitted an error for every item processed while still reporting success.</li>
<li>Opening your home directory as a workspace no longer drives the extension host to exhaustion. Typing an <code>@</code> mention indexed every file beneath it and re-ranked the whole index on each keystroke.</li>
<li>The "Supports Images" checkbox in OpenAI Compatible model settings now stays where you put it. The checkbox rendered the last committed value, and the re-sync that arrived during the save round-trip was re-emitted as a change event that wrote the old value back over your edit.</li>
<li>The error shown to the model when an <code>editor</code> call omits the text to replace now names the file and explains how to recover. The previous message was terse enough that some models re-sent the identical call until the task stopped.</li>
<li>Credentials are now stripped of invisible characters when saved, not only when pasted, and the cleanup covers AWS, GCP, and SAP fields and custom header values in addition to API keys.</li>
<li>Cline Pass now defaults to a model from your subscription rather than a free one. Its model list contains both tiers and the default was whichever model was published most recently, so a subscriber who never picked a model could be left on the free tier.</li>
<li>Cline Pass and free models now show no cost rather than the underlying market price, which is not what you are billed for.</li>
<li>Model pickers now fall back to the full Recommended, Free, and Subscribed lists when the models endpoint cannot be reached. The offline fallback was a short hardcoded list with no subscription tier at all.</li>
<li>Model lists for providers sharing the built-in catalog now refresh from the live catalog, so newly published models appear without an extension update, with timeouts so an unresponsive provider endpoint cannot stall the list.</li>
<li>Claude Code and OpenCode no longer ask for an API key they never read. Both authenticate from their own local CLI's credentials, but they were treated as key-based providers; the workaround was storing a dummy key. A missing CLI on <code>PATH</code> now warns rather than blocking, since a configured path or bundled binary also works.</li>
<li>The OpenAI Codex (ChatGPT subscription) model list no longer offers models the backend rejects, and Codex context limits are applied to every Codex model instead of being inherited from the OpenAI API catalog, which inflated both the context budget and the usage figures derived from it.</li>
<li>Models served by OpenCode Go are now sent over the wire protocol each one actually speaks. Every model was sent over the OpenAI chat-completions adapter, so models on that endpoint speaking other protocols failed or misbehaved.</li>
<li>Task history no longer goes blank when a task spawns many subagents. Subagent rows crowded out the tasks that created them, hiding the parent task and everything older.</li>
<li>Langfuse tracing, when configured, is now limited to the Cline and Cline Pass providers. The provider was ignored, so prompts and responses sent to third-party and bring-your-own-key providers were exported too.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Web search is now enabled by default on models that support it, outside YOLO mode. It can still be turned off in settings, and a settings file that cannot be read leaves it disabled rather than silently on.</li>
<li>The <code>run_commands</code> tool description now names the PowerShell edition in use — <code>Windows PowerShell (powershell.exe)</code> versus <code>PowerShell (pwsh.exe)</code> — quotes its guidance against that executable, and tells the model to run commands directly rather than wrapping them in another shell invocation. It also no longer describes the environment as Windows when <code>pwsh</code> is the configured shell on macOS or Linux.</li>
<li>Refreshed the built-in model catalog. Adds four providers (Infer by Flow7, Melious, NaN, and Wallaby) and takes the catalog from 5,788 to 6,079 models. This is a wide refresh: the resolved default model changes for 44 providers, most of them landing on DeepSeek V4.1 Flash — among them Hugging Face, Fireworks, Requesty, Nebius, Cortecs, CrossModel, DigitalOcean, Eden AI, and OpenCode Go. Gemini and Vertex now resolve to Gemini 3.8 Flash, GitHub Copilot and Vivgrid to GPT-6 Astra, and NVIDIA to GLM 5.3 Flash. If you use a provider without pinning a model, expect a different default.</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/v4.1.18...v4.1.19"><tt>v4.1.18...v4.1.19</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-17T07:51:58.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles臨時檔案託管：檔案「title.txt」開放下載且於59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/desktop-v0.0.31</link>
    <itunes:title><![CDATA[tmpfiles臨時檔案託管：檔案「title.txt」開放下載且於59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>Cline can now work on a remote machine over SSH, with the app itself staying local. Add and test a host under <strong>Settings → Remote</strong>, then pick it from the environment selector beside the workspace picker on the welcome screen — saving or testing a profile does not activate it. The connection opens at the remote user's home directory; <strong>Add project…</strong> in the workspace picker browses that machine for a project, and <strong>Local</strong> disconnects. Agent tools, workspace discovery, Git metadata, and session persistence all run on the host while approvals and live session events return to the desktop, because only the authenticated Cline Hub protocol is forwarded through the tunnel. SSH config aliases work: leave <strong>Port</strong> blank to use the alias's own configuration, or enter one to override it. Recent and last-used workspaces are remembered separately per host. Host metadata lives at <code>~/.cline/data/settings/remote-environments.json</code> (mode <code>0600</code>) and records an identity-file path, never private-key contents; the host key must already be trusted by your SSH client, and unknown or changed keys are rejected. On first connect Cline uploads a self-contained helper to <code>~/.cline/remote/</code> — no <code>apt</code>, <code>npm</code>, root access, global CLI install, or public port — and caches it for faster reconnects, leaving any existing Cline CLI hub on that account running. Linux x64 and arm64 hosts are supported (32-bit Raspberry Pi operating systems are not; macOS hosts need a locally built helper via <code>CLINE_REMOTE_HELPER_BINARY</code>). File attachments and opening a remote file in a local editor are not available in remote sessions yet — text, images, file mentions and search, Git branch operations, session history, and remote agent tools are</li>
<li>Sub-agents spawned in the same step now run at the same time instead of one after another, so delegating three independent pieces of work takes as long as the slowest one rather than the sum of all three. Tools that must run in order still do, and only back-to-back parallel calls overlap</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.30...desktop-v0.0.31"><tt>desktop-v0.0.30...desktop-v0.0.31</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>Cline can now work on a remote machine over SSH, with the app itself staying local. Add and test a host under <strong>Settings → Remote</strong>, then pick it from the environment selector beside the workspace picker on the welcome screen — saving or testing a profile does not activate it. The connection opens at the remote user's home directory; <strong>Add project…</strong> in the workspace picker browses that machine for a project, and <strong>Local</strong> disconnects. Agent tools, workspace discovery, Git metadata, and session persistence all run on the host while approvals and live session events return to the desktop, because only the authenticated Cline Hub protocol is forwarded through the tunnel. SSH config aliases work: leave <strong>Port</strong> blank to use the alias's own configuration, or enter one to override it. Recent and last-used workspaces are remembered separately per host. Host metadata lives at <code>~/.cline/data/settings/remote-environments.json</code> (mode <code>0600</code>) and records an identity-file path, never private-key contents; the host key must already be trusted by your SSH client, and unknown or changed keys are rejected. On first connect Cline uploads a self-contained helper to <code>~/.cline/remote/</code> — no <code>apt</code>, <code>npm</code>, root access, global CLI install, or public port — and caches it for faster reconnects, leaving any existing Cline CLI hub on that account running. Linux x64 and arm64 hosts are supported (32-bit Raspberry Pi operating systems are not; macOS hosts need a locally built helper via <code>CLINE_REMOTE_HELPER_BINARY</code>). File attachments and opening a remote file in a local editor are not available in remote sessions yet — text, images, file mentions and search, Git branch operations, session history, and remote agent tools are</li>
<li>Sub-agents spawned in the same step now run at the same time instead of one after another, so delegating three independent pieces of work takes as long as the slowest one rather than the sum of all three. Tools that must run in order still do, and only back-to-back parallel calls overlap</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.30...desktop-v0.0.31"><tt>desktop-v0.0.30...desktop-v0.0.31</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>Cline can now work on a remote machine over SSH, with the app itself staying local. Add and test a host under <strong>Settings → Remote</strong>, then pick it from the environment selector beside the workspace picker on the welcome screen — saving or testing a profile does not activate it. The connection opens at the remote user's home directory; <strong>Add project…</strong> in the workspace picker browses that machine for a project, and <strong>Local</strong> disconnects. Agent tools, workspace discovery, Git metadata, and session persistence all run on the host while approvals and live session events return to the desktop, because only the authenticated Cline Hub protocol is forwarded through the tunnel. SSH config aliases work: leave <strong>Port</strong> blank to use the alias's own configuration, or enter one to override it. Recent and last-used workspaces are remembered separately per host. Host metadata lives at <code>~/.cline/data/settings/remote-environments.json</code> (mode <code>0600</code>) and records an identity-file path, never private-key contents; the host key must already be trusted by your SSH client, and unknown or changed keys are rejected. On first connect Cline uploads a self-contained helper to <code>~/.cline/remote/</code> — no <code>apt</code>, <code>npm</code>, root access, global CLI install, or public port — and caches it for faster reconnects, leaving any existing Cline CLI hub on that account running. Linux x64 and arm64 hosts are supported (32-bit Raspberry Pi operating systems are not; macOS hosts need a locally built helper via <code>CLINE_REMOTE_HELPER_BINARY</code>). File attachments and opening a remote file in a local editor are not available in remote sessions yet — text, images, file mentions and search, Git branch operations, session history, and remote agent tools are</li>
<li>Sub-agents spawned in the same step now run at the same time instead of one after another, so delegating three independent pieces of work takes as long as the slowest one rather than the sum of all three. Tools that must run in order still do, and only back-to-back parallel calls overlap</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.30...desktop-v0.0.31"><tt>desktop-v0.0.30...desktop-v0.0.31</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-17T22:08:55.000Z</pubDate>
  </item><item>
    <title><![CDATA[/tmp/files臨時檔案託管：title.txt檔案提供下載 59分鐘後過期失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/desktop-v0.0.30</link>
    <itunes:title><![CDATA[/tmp/files臨時檔案託管：title.txt檔案提供下載 59分鐘後過期失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>Windows updates no longer fail with "Error opening file for writing". The installer hook meant to stop the running backend never terminated anything: Tauri ships a 32-bit NSIS installer, so its PowerShell ran under WOW64, where <code>Get-Process</code> reports an empty path for every 64-bit process — the filter matched no sidecar on any x64 machine. The installer then wrote over an executable the detached Hub daemon still held open, which is why updating from 0.0.25 through 0.0.28 failed exactly as 0.0.24 did. The installer now asks the Windows Restart Manager which processes hold this install's files and shuts those down; that is bitness-independent, covers the connector processes the Hub spawns, and leaves a side-by-side Cline Beta alone. If Restart Manager itself fails you get a Retry/Cancel prompt rather than a half-replaced install</li>
<li>On Windows, Cline no longer runs a program out of your workspace when it means to run the system one. Windows resolves a bare program name through the child process's working directory before it searches PATH, and Cline runs <code>rg</code>, <code>git</code>, and <code>powershell</code> with your workspace as that directory — so a repository that happened to contain its own <code>rg.exe</code> would have that copy executed as soon as the workspace was indexed. Cline now opts out of current-directory executable search at startup, and every process it spawns inherits the setting</li>
<li>Long sessions now compact when they actually need to rather than running out of room. The trigger compared a character-based estimate of the transcript (~3 characters per token) against the model's context limit, so content that tokenizes far denser than that — disassembly, image dumps, minified sources — could fill the real context window while the estimate stayed under the threshold and compaction never fired; the turn was then squeezed down to a handful of output tokens. The trigger now also uses the token count the provider itself reports, and how much history is kept is scaled by how far off the estimate turned out to be. The summarizer's output budget also doubled, so a model that reasons before answering can no longer spend the whole budget thinking and return no summary at all</li>
<li>A tool that streams its output no longer keeps spinning after it has finished. When a tool streamed everything as it went, its completion event carried no final output, which chat read as "still running" — so the tool kept its running presentation, spinner included, for the rest of the turn</li>
<li>Refreshed Cline's logo and app icons throughout the app, the Dock and taskbar, and the installer</li>
<li>The CoreWeave provider's branding and setup links now match CoreWeave's current documentation, and Weights &amp; Biases gained a direct link to its API key page from provider setup</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.29...desktop-v0.0.30"><tt>desktop-v0.0.29...desktop-v0.0.30</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>Windows updates no longer fail with "Error opening file for writing". The installer hook meant to stop the running backend never terminated anything: Tauri ships a 32-bit NSIS installer, so its PowerShell ran under WOW64, where <code>Get-Process</code> reports an empty path for every 64-bit process — the filter matched no sidecar on any x64 machine. The installer then wrote over an executable the detached Hub daemon still held open, which is why updating from 0.0.25 through 0.0.28 failed exactly as 0.0.24 did. The installer now asks the Windows Restart Manager which processes hold this install's files and shuts those down; that is bitness-independent, covers the connector processes the Hub spawns, and leaves a side-by-side Cline Beta alone. If Restart Manager itself fails you get a Retry/Cancel prompt rather than a half-replaced install</li>
<li>On Windows, Cline no longer runs a program out of your workspace when it means to run the system one. Windows resolves a bare program name through the child process's working directory before it searches PATH, and Cline runs <code>rg</code>, <code>git</code>, and <code>powershell</code> with your workspace as that directory — so a repository that happened to contain its own <code>rg.exe</code> would have that copy executed as soon as the workspace was indexed. Cline now opts out of current-directory executable search at startup, and every process it spawns inherits the setting</li>
<li>Long sessions now compact when they actually need to rather than running out of room. The trigger compared a character-based estimate of the transcript (~3 characters per token) against the model's context limit, so content that tokenizes far denser than that — disassembly, image dumps, minified sources — could fill the real context window while the estimate stayed under the threshold and compaction never fired; the turn was then squeezed down to a handful of output tokens. The trigger now also uses the token count the provider itself reports, and how much history is kept is scaled by how far off the estimate turned out to be. The summarizer's output budget also doubled, so a model that reasons before answering can no longer spend the whole budget thinking and return no summary at all</li>
<li>A tool that streams its output no longer keeps spinning after it has finished. When a tool streamed everything as it went, its completion event carried no final output, which chat read as "still running" — so the tool kept its running presentation, spinner included, for the rest of the turn</li>
<li>Refreshed Cline's logo and app icons throughout the app, the Dock and taskbar, and the installer</li>
<li>The CoreWeave provider's branding and setup links now match CoreWeave's current documentation, and Weights &amp; Biases gained a direct link to its API key page from provider setup</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.29...desktop-v0.0.30"><tt>desktop-v0.0.29...desktop-v0.0.30</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>Windows updates no longer fail with "Error opening file for writing". The installer hook meant to stop the running backend never terminated anything: Tauri ships a 32-bit NSIS installer, so its PowerShell ran under WOW64, where <code>Get-Process</code> reports an empty path for every 64-bit process — the filter matched no sidecar on any x64 machine. The installer then wrote over an executable the detached Hub daemon still held open, which is why updating from 0.0.25 through 0.0.28 failed exactly as 0.0.24 did. The installer now asks the Windows Restart Manager which processes hold this install's files and shuts those down; that is bitness-independent, covers the connector processes the Hub spawns, and leaves a side-by-side Cline Beta alone. If Restart Manager itself fails you get a Retry/Cancel prompt rather than a half-replaced install</li>
<li>On Windows, Cline no longer runs a program out of your workspace when it means to run the system one. Windows resolves a bare program name through the child process's working directory before it searches PATH, and Cline runs <code>rg</code>, <code>git</code>, and <code>powershell</code> with your workspace as that directory — so a repository that happened to contain its own <code>rg.exe</code> would have that copy executed as soon as the workspace was indexed. Cline now opts out of current-directory executable search at startup, and every process it spawns inherits the setting</li>
<li>Long sessions now compact when they actually need to rather than running out of room. The trigger compared a character-based estimate of the transcript (~3 characters per token) against the model's context limit, so content that tokenizes far denser than that — disassembly, image dumps, minified sources — could fill the real context window while the estimate stayed under the threshold and compaction never fired; the turn was then squeezed down to a handful of output tokens. The trigger now also uses the token count the provider itself reports, and how much history is kept is scaled by how far off the estimate turned out to be. The summarizer's output budget also doubled, so a model that reasons before answering can no longer spend the whole budget thinking and return no summary at all</li>
<li>A tool that streams its output no longer keeps spinning after it has finished. When a tool streamed everything as it went, its completion event carried no final output, which chat read as "still running" — so the tool kept its running presentation, spinner included, for the rest of the turn</li>
<li>Refreshed Cline's logo and app icons throughout the app, the Dock and taskbar, and the installer</li>
<li>The CoreWeave provider's branding and setup links now match CoreWeave's current documentation, and Weights &amp; Biases gained a direct link to its API key page from provider setup</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.29...desktop-v0.0.30"><tt>desktop-v0.0.29...desktop-v0.0.30</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-17T03:35:26.000Z</pubDate>
  </item><item>
    <title><![CDATA[/tmp/files暫存空間：title.txt開放下載，檔案將於59分鐘後失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/desktop-v0.0.29</link>
    <itunes:title><![CDATA[/tmp/files暫存空間：title.txt開放下載，檔案將於59分鐘後失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>Queued messages can now be sent into the running turn with Enter. While the agent is working, pressing Enter with an empty composer steers the first message in the queue, so it is picked up at the next turn boundary instead of waiting for the whole task to finish; the composer placeholder says so whenever the queue is non-empty. The queue head is claimed atomically, so a fast second Enter can't steer a message that has already left the queue, and a steer that fails now surfaces as a toast rather than silently doing nothing</li>
<li>The macOS Dock icon now has the standard margin other apps use. The bundled icon and the selectable runtime icons (Classic, Chip, Hologram, Midnight) were edge-to-edge, so Cline rendered visibly larger than its neighbours in the Dock. Windows keeps its existing artwork</li>
<li>The session sidebar's sort toggle now shows the mode a click switches to, not the one already active — a folder icon while sorted by time, a clock while grouped by project</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.28...desktop-v0.0.29"><tt>desktop-v0.0.28...desktop-v0.0.29</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>Queued messages can now be sent into the running turn with Enter. While the agent is working, pressing Enter with an empty composer steers the first message in the queue, so it is picked up at the next turn boundary instead of waiting for the whole task to finish; the composer placeholder says so whenever the queue is non-empty. The queue head is claimed atomically, so a fast second Enter can't steer a message that has already left the queue, and a steer that fails now surfaces as a toast rather than silently doing nothing</li>
<li>The macOS Dock icon now has the standard margin other apps use. The bundled icon and the selectable runtime icons (Classic, Chip, Hologram, Midnight) were edge-to-edge, so Cline rendered visibly larger than its neighbours in the Dock. Windows keeps its existing artwork</li>
<li>The session sidebar's sort toggle now shows the mode a click switches to, not the one already active — a folder icon while sorted by time, a clock while grouped by project</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.28...desktop-v0.0.29"><tt>desktop-v0.0.28...desktop-v0.0.29</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>Queued messages can now be sent into the running turn with Enter. While the agent is working, pressing Enter with an empty composer steers the first message in the queue, so it is picked up at the next turn boundary instead of waiting for the whole task to finish; the composer placeholder says so whenever the queue is non-empty. The queue head is claimed atomically, so a fast second Enter can't steer a message that has already left the queue, and a steer that fails now surfaces as a toast rather than silently doing nothing</li>
<li>The macOS Dock icon now has the standard margin other apps use. The bundled icon and the selectable runtime icons (Classic, Chip, Hologram, Midnight) were edge-to-edge, so Cline rendered visibly larger than its neighbours in the Dock. Windows keeps its existing artwork</li>
<li>The session sidebar's sort toggle now shows the mode a click switches to, not the one already active — a folder icon while sorted by time, a clock while grouped by project</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/desktop-v0.0.28...desktop-v0.0.29"><tt>desktop-v0.0.28...desktop-v0.0.29</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-16T17:21:11.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles臨時檔案空間：title.txt下載頁面與限時過期資訊]]></title>
    <link>https://github.com/cline/cline/releases/tag/v4.1.18</link>
    <itunes:title><![CDATA[tmpfiles臨時檔案空間：title.txt下載頁面與限時過期資訊]]></itunes:title>
    <itunes:author><![CDATA[saoudrizwan]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>The ext-sdk-bundle-rollout flag reached 100%, so stable releases now ship a<br>
standalone build of main through ext-vscode-publish-stable.yml rather than the<br>
combined legacy+next A/B VSIX. Rewrite the skill around that path and record<br>
what is left to retire.</p>
<p>Also corrects two things the old text got wrong: nightly is manual-dispatch<br>
only (the cron was removed because PublishNightly gained required reviewers,<br>
so unattended runs sat waiting and starved their successors), and the stable<br>
workflow tags before it builds, so a tag-push failure fails early rather than<br>
leaving a published-but-untagged release.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>The ext-sdk-bundle-rollout flag reached 100%, so stable releases now ship a<br>
standalone build of main through ext-vscode-publish-stable.yml rather than the<br>
combined legacy+next A/B VSIX. Rewrite the skill around that path and record<br>
what is left to retire.</p>
<p>Also corrects two things the old text got wrong: nightly is manual-dispatch<br>
only (the cron was removed because PublishNightly gained required reviewers,<br>
so unattended runs sat waiting and starved their successors), and the stable<br>
workflow tags before it builds, so a tag-push failure fails early rather than<br>
leaving a published-but-untagged release.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>The ext-sdk-bundle-rollout flag reached 100%, so stable releases now ship a<br>
standalone build of main through ext-vscode-publish-stable.yml rather than the<br>
combined legacy+next A/B VSIX. Rewrite the skill around that path and record<br>
what is left to retire.</p>
<p>Also corrects two things the old text got wrong: nightly is manual-dispatch<br>
only (the cron was removed because PublishNightly gained required reviewers,<br>
so unattended runs sat waiting and starved their successors), and the stable<br>
workflow tags before it builds, so a tag-push failure fails early rather than<br>
leaving a published-but-untagged release.</p>]]></content:encoded>
    
    <pubDate>2026-09-15T06:56:07.000Z</pubDate>
  </item><item>
    <title><![CDATA[暫存檔案託管平台tmpfiles提供title.txt下載　檔案將於59分鐘後過期]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fsdk%2Fv0.0.83</link>
    <itunes:title><![CDATA[暫存檔案託管平台tmpfiles提供title.txt下載　檔案將於59分鐘後過期]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<ul>
<li>Hub-managed Agent Plugins. Packages under <code>~/.agents/plugins/*</code> on the hub host are discovered and validated from their root <code>plugin.json</code>; valid skills under <code>skills/</code> are exposed through the skills tool as <code>plugin-name:skill-name</code>, and stdio, Streamable HTTP, and legacy SSE servers from <code>mcp.json</code> are started without touching <code>cline_mcp_settings.json</code>. Workspace <code>.agents/plugins</code> directories are deliberately not scanned, so opening a repo cannot implicitly start repo-controlled MCP servers; extra roots require an explicit <code>agentPluginPaths</code>. Enablement lives in hub settings keyed by manifest name and publishes <code>settings.changed</code>, so clients no longer need their own loader or enablement store. Two bugs fixed along the way: <code>settings.toggle({type: "skills"})</code> wrote a <code>disabled</code> key into a plugin skill's SKILL.md frontmatter, which the strict Agent Skills parser then rejected so the skill silently vanished until hand-edited; and <code>InMemoryMcpManager.dispose()</code> aborted on the first <code>disconnect()</code> rejection, leaking every remaining server's process</li>
<li>A model turn that dies mid-stream with a transient provider error is now retried up to 3 times with exponential backoff instead of failing the whole run — a single forwarded 429 previously aborted the run outright. Retryability is read from the AI SDK's typed signals, and a turn is never retried once it has streamed any text, reasoning, media, or tool call, so nothing is duplicated. Model calls also now allow 5 SDK-level retries for request-start 429/5xx/network failures, up from 2</li>
<li>Streaming is no longer throttled by hook forwarding. The hub proxied every runtime event to client-contributed <code>onEvent</code> hooks as a capability round trip carrying the full session snapshot, with the agent loop awaiting it — so each streamed token cost a few hundred KB of serialization, a persisted row, and a blocking IPC hop. Per-chunk text, reasoning, and tool-update deltas are no longer forwarded to remote <code>onEvent</code> hooks; every other event still reaches hooks unchanged. The hub event log also moved to <code>synchronous = NORMAL</code>, dropping one fsync per appended delta</li>
<li>Checkpoints no longer re-hash unchanged untracked files on every turn. Each turn built a throwaway git index, so git re-read every untracked file before each model call — with multi-GB untracked data in the workspace this blocked every message for seconds to minutes. A persistent per-session snapshot index lets git's stat cache skip unchanged files, so from the second turn a snapshot costs about one git process. Snapshot contents are byte-identical to before. A corrupt index now heals with one rebuild-and-retry that also clears a stale <code>index.lock</code>, where previously a git process killed mid-add degraded that session to HEAD-only checkpoints permanently</li>
<li><code>run_commands</code> no longer hangs when a command backgrounds a child. The executor settled on the child's <code>close</code> event, which waits for the stdio pipes to drain; a backgrounded process (<code>cmd &amp;</code>, <code>nohup</code>) holds the inherited write-ends open, so a finished command hung until the timeout killed the whole process tree. It now also settles on <code>exit</code> after a one-second grace period, reporting the exit code and the output collected so far</li>
<li><code>apply_patch</code> "Add File" against a path that already exists is now rejected instead of silently overwriting it. Only UPDATE and DELETE targets were pre-loaded, so the parser's "File already exists" guard never saw ADD targets and the patch destroyed the file's contents with no error and no diff of what was lost</li>
<li>Nested PowerShell invocations are now unwrapped instead of being double-parsed. Command text is fed to PowerShell through a stdin bootstrap that the outer shell parses as PowerShell source, so a command written as <code>powershell -Command "... $_ ..."</code> had its argument interpolated before the nested shell ran — pipelines using <code>$_</code> emitted one error per enumerated item, an error flood that looked like a hang, while the child still exited 0. Unwrapping is applied only when it is semantics-preserving: same PowerShell edition, <code>-NoProfile</code>, and an entirely quoted <code>-Command</code> tail; everything else passes through byte-identical</li>
<li>The <code>run_commands</code> tool description now names the actual PowerShell edition in use — <code>Windows PowerShell (powershell.exe)</code> versus <code>PowerShell (pwsh.exe)</code> or <code>PowerShell (pwsh)</code> — quotes its guidance against the resolved executable, and tells the model to write commands directly rather than wrapping them in another <code>-Command</code> or <code>/c</code> invocation. It also no longer claims "in Windows environment" when <code>pwsh</code> is the configured shell on macOS or Linux</li>
<li>No file index is built when the workspace root is the home directory or a filesystem root. Running from <code>$HOME</code> and typing an <code>@</code> mention listed every file under home and re-ranked the whole index on each keystroke, driving the process to many GB of RSS until it was OOM-killed. Paths are canonicalized first so symlinked or differently-cased spellings still hit the guard</li>
<li>Credential fields are now stripped of Unicode control and format characters and surrounding whitespace before being saved. A key pasted with an invisible character (BOM, zero-width space, bidi mark) was stored corrupted and the provider returned a 401 indistinguishable from a genuinely wrong key, while masked rendering hid the corruption</li>
<li>The <code>editor</code> tool's error for a null <code>old_text</code> now names the file, says whether the parameter was null or omitted, and spells out the recovery. Models that fill optional parameters with null hit a terse "required" message and re-sent the identical call until the loop detector hard-stopped the run</li>
<li>Provider-native web search is now enabled by default in non-yolo sessions on supported provider/model combinations, instead of requiring <code>tools.web_search.enabled = true</code>. An explicit <code>false</code> still opts out, and the settings loader fails closed: web search stays disabled when a global settings file exists but cannot be read or parsed</li>
<li>Session history no longer hides root sessions behind their children. The list over-fetched a fixed window of rows and filtered out subagent and team-task children client-side; since children sort after the root that spawned them, one session with more children than the window hid itself and every older root. Listing now widens until the requested page fills, and the backends filter child rows in SQL</li>
<li>Schedules no longer stall each other. The cron runner awaited the whole claimed batch, so one long agent turn blocked dispatch of every other schedule; dispatch is now decoupled from execution, and per-schedule and global parallelism are enforced inside the SQLite claim transaction so multiple connections cannot exceed a schedule's limit. Locally active claims are renewed each tick, so work resumed after system sleep is not reclaimed and started twice, and terminal status is written before the markdown report so a failed report write cannot replay a completed turn</li>
<li>Recurring schedules created without a timezone now persist the host's local IANA timezone rather than an implicit default, and the scheduling tool prompts tell the model to omit the timezone instead of guessing</li>
<li>Automation event acceptance is now atomic. The event log row was committed before specs were matched and runs materialized, so a failure mid-fan-out left the event permanently deduplicated and impossible to redeliver, and other connections could observe partially materialized runs. Acceptance now runs in one write transaction, and failures roll back and propagate to the caller for redelivery</li>
<li>New <code>RemoteEnvironmentService</code> runs sessions on an SSH host while the client stays local. <code>connect()</code> inspects the remote host, uploads a self-contained helper binary to <code>~/.cline/remote/</code>, starts a loopback-bound Hub there, and opens an SSH tunnel to it; the returned endpoint and token are passed to <code>ClineCore.create({ backendMode: "remote" })</code>, after which tools, sessions, and persistence all execute remotely. SSH runs with <code>BatchMode=yes</code> and <code>StrictHostKeyChecking=yes</code>, so the host key must already be known and password authentication is not supported; profiles are stored at mode 0600 and hold identity-file paths only, never secrets. Remote targets are Linux and macOS on x64/arm64, and embedders must supply a helper binary cross-compiled for the target. This is an SDK building block — no CLI command or desktop UI is wired to it yet, and it is inert unless an embedder constructs the service</li>
<li>A session started without an explicit <code>systemPrompt</code> now gets a default Cline prompt built on the execution host, rather than running with none. Callers that pass their own prompt are unaffected</li>
<li>Hub daemons can now be started with connector management disabled, so an SSH-spawned Hub does not adopt or restart the account's Slack and Telegram connectors. Locally started hubs are unaffected</li>
<li>Hub clients can now authenticate a remote connection with HTTP headers via a <code>resolveConnectionHeaders()</code> resolver, re-resolved on every reconnect so short-lived credentials can refresh. It is mutually exclusive with the local hub-token subprotocol. <code>NodeHubClient.connect()</code> was also reworked so concurrent calls share one in-flight promise and a stale registration or connect timeout can no longer clobber a newer attempt</li>
<li>A hub running the same core version as the client no longer prompts to update. Two artifacts of the same release cut from different commits never share a build fingerprint, so anyone with both the desktop app and the CLI installed got a "Cline Hub was updated" dialog on every launch whose "Update and restart" looped on "no app update available". Genuinely different core releases still prompt</li>
<li>Sessions seeded with history (forks, checkpoint restores) now persist their live idle status instead of defaulting to "running", which left a restored session showing as busy forever</li>
<li>Sessions imported from Claude Code, Codex, or opencode now summarize the foreign history on first resume rather than replaying tool calls the current agent cannot make. The summary is persisted so it runs once, the canonical transcript is left intact, and a failed attempt falls back to the raw history. Imported sessions also record an import origin that is stamped on their telemetry, and resuming a session no longer overwrites stored import or automation provenance with a default "user" origin</li>
<li>Session import source directories now resolve correctly on Windows profiles where <code>USERPROFILE</code> disagrees with what Node reports, and honor <code>CLAUDE_CONFIG_DIR</code>, <code>CODEX_HOME</code>, and <code>XDG_DATA_HOME</code></li>
<li>Cline Pass and free models now report zero cost. The backend reports upstream market cost on those responses and it was passed straight through, so subscription and free usage showed dollar figures for requests that are not billed per token</li>
<li>Offline clients now fall back to generated Recommended, Free, and Subscribed model lists built at release time. The fallback was a hand-maintained literal of six models with an empty Cline Pass tier, so when the recommended-models endpoint was unreachable, subscribers got no subscribed tier at all</li>
<li>Langfuse tracing is now limited to the Cline and Cline Pass providers. The provider argument was ignored, so in Langfuse-configured environments prompts and responses from third-party and BYOK providers were exported too</li>
<li>OpenCode Go models are now routed to the right wire protocol. One base URL serves several protocols, but every model was sent over the OpenAI chat-completions adapter, so Anthropic-protocol and OpenAI Responses models on that endpoint failed or misbehaved. Requests also now carry the sticky session header and a Cline user agent</li>
<li>Claude Code and OpenCode are now declared local-auth providers with a named local CLI, so hosts route them to a local-CLI readiness screen instead of demanding an API key they never read — previously the workaround was storing a dummy key, and the desktop app rendered an OAuth button for OpenCode that did nothing</li>
<li>The OpenAI Codex (ChatGPT subscription) model list no longer gets overwritten with the full OpenAI API catalog by hosts that query provider models, which also lost the Codex context caps. The Codex backend's 400K/272K/128K budget is now clamped onto every Codex model rather than just one, so newer models no longer inherit the API catalog's 1M limits and produce over-long requests and wrong context math. Retired ChatGPT-account models were dropped from eligibility and the default moved to <code>gpt-5.6-terra</code></li>
<li>Live model catalogs now refresh for every shared-catalog provider, not just Cline and Cline Pass — other providers' lists were whatever was bundled at build time, so newly published models never appeared without a package update. The catalog fetch and the model-source fetch are both bounded by timeouts, so a hung models endpoint can no longer stall the provider list indefinitely</li>
<li>Providers are now reported as configured based on the provider their credentials are stored under. Cline Pass signs in as Cline and never writes its own entry, so a new Cline Pass user saw only one configured provider despite one sign-in configuring both. The "(free)" suffix now also applies to free-tier models whose ids come from upstream rather than Cline's own namespace</li>
<li><code>@cline/ui</code> gains a shared <code>Switch</code> built on a native checkbox, and <code>SearchCombobox</code> keeps section headers visible while searching and can refresh its options each time it opens. Migrators should note the <code>Switch</code> has no <code>data-state</code>/<code>asChild</code> hooks and tests must read the native <code>checked</code> property rather than <code>aria-checked</code></li>
<li>Cline Pass now defaults to a subscribed-tier model. Its provider list mixes subscribed (<code>cline-pass/*</code>) and free models, and the default was simply the first entry in a release-date-ordered list, so it drifted onto whichever free model had shipped most recently — a subscriber who never picked a model was put on the free tier rather than the one they pay for. The default is now restricted to <code>cline-pass/*</code> ids</li>
<li>Refreshed the model catalog. Adds four providers (Infer by Flow7, Melious, NaN, and Wallaby) and takes the bundled catalog from 5,788 to 6,079 models. This is a wide refresh: the resolved default model changes for 44 providers, most of them landing on DeepSeek V4.1 Flash — among them Hugging Face, Fireworks, Requesty, Nebius, Cortecs, CrossModel, DigitalOcean, Eden AI, and OpenCode Go. Gemini and Vertex now resolve to Gemini 3.8 Flash, GitHub Copilot and Vivgrid to GPT-6 Astra, and NVIDIA to GLM 5.3 Flash. If you use any provider without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.82...sdk/sdk/v0.0.83"><tt>sdk/sdk/v0.0.82...sdk/sdk/v0.0.83</tt></a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<ul>
<li>Hub-managed Agent Plugins. Packages under <code>~/.agents/plugins/*</code> on the hub host are discovered and validated from their root <code>plugin.json</code>; valid skills under <code>skills/</code> are exposed through the skills tool as <code>plugin-name:skill-name</code>, and stdio, Streamable HTTP, and legacy SSE servers from <code>mcp.json</code> are started without touching <code>cline_mcp_settings.json</code>. Workspace <code>.agents/plugins</code> directories are deliberately not scanned, so opening a repo cannot implicitly start repo-controlled MCP servers; extra roots require an explicit <code>agentPluginPaths</code>. Enablement lives in hub settings keyed by manifest name and publishes <code>settings.changed</code>, so clients no longer need their own loader or enablement store. Two bugs fixed along the way: <code>settings.toggle({type: "skills"})</code> wrote a <code>disabled</code> key into a plugin skill's SKILL.md frontmatter, which the strict Agent Skills parser then rejected so the skill silently vanished until hand-edited; and <code>InMemoryMcpManager.dispose()</code> aborted on the first <code>disconnect()</code> rejection, leaking every remaining server's process</li>
<li>A model turn that dies mid-stream with a transient provider error is now retried up to 3 times with exponential backoff instead of failing the whole run — a single forwarded 429 previously aborted the run outright. Retryability is read from the AI SDK's typed signals, and a turn is never retried once it has streamed any text, reasoning, media, or tool call, so nothing is duplicated. Model calls also now allow 5 SDK-level retries for request-start 429/5xx/network failures, up from 2</li>
<li>Streaming is no longer throttled by hook forwarding. The hub proxied every runtime event to client-contributed <code>onEvent</code> hooks as a capability round trip carrying the full session snapshot, with the agent loop awaiting it — so each streamed token cost a few hundred KB of serialization, a persisted row, and a blocking IPC hop. Per-chunk text, reasoning, and tool-update deltas are no longer forwarded to remote <code>onEvent</code> hooks; every other event still reaches hooks unchanged. The hub event log also moved to <code>synchronous = NORMAL</code>, dropping one fsync per appended delta</li>
<li>Checkpoints no longer re-hash unchanged untracked files on every turn. Each turn built a throwaway git index, so git re-read every untracked file before each model call — with multi-GB untracked data in the workspace this blocked every message for seconds to minutes. A persistent per-session snapshot index lets git's stat cache skip unchanged files, so from the second turn a snapshot costs about one git process. Snapshot contents are byte-identical to before. A corrupt index now heals with one rebuild-and-retry that also clears a stale <code>index.lock</code>, where previously a git process killed mid-add degraded that session to HEAD-only checkpoints permanently</li>
<li><code>run_commands</code> no longer hangs when a command backgrounds a child. The executor settled on the child's <code>close</code> event, which waits for the stdio pipes to drain; a backgrounded process (<code>cmd &amp;</code>, <code>nohup</code>) holds the inherited write-ends open, so a finished command hung until the timeout killed the whole process tree. It now also settles on <code>exit</code> after a one-second grace period, reporting the exit code and the output collected so far</li>
<li><code>apply_patch</code> "Add File" against a path that already exists is now rejected instead of silently overwriting it. Only UPDATE and DELETE targets were pre-loaded, so the parser's "File already exists" guard never saw ADD targets and the patch destroyed the file's contents with no error and no diff of what was lost</li>
<li>Nested PowerShell invocations are now unwrapped instead of being double-parsed. Command text is fed to PowerShell through a stdin bootstrap that the outer shell parses as PowerShell source, so a command written as <code>powershell -Command "... $_ ..."</code> had its argument interpolated before the nested shell ran — pipelines using <code>$_</code> emitted one error per enumerated item, an error flood that looked like a hang, while the child still exited 0. Unwrapping is applied only when it is semantics-preserving: same PowerShell edition, <code>-NoProfile</code>, and an entirely quoted <code>-Command</code> tail; everything else passes through byte-identical</li>
<li>The <code>run_commands</code> tool description now names the actual PowerShell edition in use — <code>Windows PowerShell (powershell.exe)</code> versus <code>PowerShell (pwsh.exe)</code> or <code>PowerShell (pwsh)</code> — quotes its guidance against the resolved executable, and tells the model to write commands directly rather than wrapping them in another <code>-Command</code> or <code>/c</code> invocation. It also no longer claims "in Windows environment" when <code>pwsh</code> is the configured shell on macOS or Linux</li>
<li>No file index is built when the workspace root is the home directory or a filesystem root. Running from <code>$HOME</code> and typing an <code>@</code> mention listed every file under home and re-ranked the whole index on each keystroke, driving the process to many GB of RSS until it was OOM-killed. Paths are canonicalized first so symlinked or differently-cased spellings still hit the guard</li>
<li>Credential fields are now stripped of Unicode control and format characters and surrounding whitespace before being saved. A key pasted with an invisible character (BOM, zero-width space, bidi mark) was stored corrupted and the provider returned a 401 indistinguishable from a genuinely wrong key, while masked rendering hid the corruption</li>
<li>The <code>editor</code> tool's error for a null <code>old_text</code> now names the file, says whether the parameter was null or omitted, and spells out the recovery. Models that fill optional parameters with null hit a terse "required" message and re-sent the identical call until the loop detector hard-stopped the run</li>
<li>Provider-native web search is now enabled by default in non-yolo sessions on supported provider/model combinations, instead of requiring <code>tools.web_search.enabled = true</code>. An explicit <code>false</code> still opts out, and the settings loader fails closed: web search stays disabled when a global settings file exists but cannot be read or parsed</li>
<li>Session history no longer hides root sessions behind their children. The list over-fetched a fixed window of rows and filtered out subagent and team-task children client-side; since children sort after the root that spawned them, one session with more children than the window hid itself and every older root. Listing now widens until the requested page fills, and the backends filter child rows in SQL</li>
<li>Schedules no longer stall each other. The cron runner awaited the whole claimed batch, so one long agent turn blocked dispatch of every other schedule; dispatch is now decoupled from execution, and per-schedule and global parallelism are enforced inside the SQLite claim transaction so multiple connections cannot exceed a schedule's limit. Locally active claims are renewed each tick, so work resumed after system sleep is not reclaimed and started twice, and terminal status is written before the markdown report so a failed report write cannot replay a completed turn</li>
<li>Recurring schedules created without a timezone now persist the host's local IANA timezone rather than an implicit default, and the scheduling tool prompts tell the model to omit the timezone instead of guessing</li>
<li>Automation event acceptance is now atomic. The event log row was committed before specs were matched and runs materialized, so a failure mid-fan-out left the event permanently deduplicated and impossible to redeliver, and other connections could observe partially materialized runs. Acceptance now runs in one write transaction, and failures roll back and propagate to the caller for redelivery</li>
<li>New <code>RemoteEnvironmentService</code> runs sessions on an SSH host while the client stays local. <code>connect()</code> inspects the remote host, uploads a self-contained helper binary to <code>~/.cline/remote/</code>, starts a loopback-bound Hub there, and opens an SSH tunnel to it; the returned endpoint and token are passed to <code>ClineCore.create({ backendMode: "remote" })</code>, after which tools, sessions, and persistence all execute remotely. SSH runs with <code>BatchMode=yes</code> and <code>StrictHostKeyChecking=yes</code>, so the host key must already be known and password authentication is not supported; profiles are stored at mode 0600 and hold identity-file paths only, never secrets. Remote targets are Linux and macOS on x64/arm64, and embedders must supply a helper binary cross-compiled for the target. This is an SDK building block — no CLI command or desktop UI is wired to it yet, and it is inert unless an embedder constructs the service</li>
<li>A session started without an explicit <code>systemPrompt</code> now gets a default Cline prompt built on the execution host, rather than running with none. Callers that pass their own prompt are unaffected</li>
<li>Hub daemons can now be started with connector management disabled, so an SSH-spawned Hub does not adopt or restart the account's Slack and Telegram connectors. Locally started hubs are unaffected</li>
<li>Hub clients can now authenticate a remote connection with HTTP headers via a <code>resolveConnectionHeaders()</code> resolver, re-resolved on every reconnect so short-lived credentials can refresh. It is mutually exclusive with the local hub-token subprotocol. <code>NodeHubClient.connect()</code> was also reworked so concurrent calls share one in-flight promise and a stale registration or connect timeout can no longer clobber a newer attempt</li>
<li>A hub running the same core version as the client no longer prompts to update. Two artifacts of the same release cut from different commits never share a build fingerprint, so anyone with both the desktop app and the CLI installed got a "Cline Hub was updated" dialog on every launch whose "Update and restart" looped on "no app update available". Genuinely different core releases still prompt</li>
<li>Sessions seeded with history (forks, checkpoint restores) now persist their live idle status instead of defaulting to "running", which left a restored session showing as busy forever</li>
<li>Sessions imported from Claude Code, Codex, or opencode now summarize the foreign history on first resume rather than replaying tool calls the current agent cannot make. The summary is persisted so it runs once, the canonical transcript is left intact, and a failed attempt falls back to the raw history. Imported sessions also record an import origin that is stamped on their telemetry, and resuming a session no longer overwrites stored import or automation provenance with a default "user" origin</li>
<li>Session import source directories now resolve correctly on Windows profiles where <code>USERPROFILE</code> disagrees with what Node reports, and honor <code>CLAUDE_CONFIG_DIR</code>, <code>CODEX_HOME</code>, and <code>XDG_DATA_HOME</code></li>
<li>Cline Pass and free models now report zero cost. The backend reports upstream market cost on those responses and it was passed straight through, so subscription and free usage showed dollar figures for requests that are not billed per token</li>
<li>Offline clients now fall back to generated Recommended, Free, and Subscribed model lists built at release time. The fallback was a hand-maintained literal of six models with an empty Cline Pass tier, so when the recommended-models endpoint was unreachable, subscribers got no subscribed tier at all</li>
<li>Langfuse tracing is now limited to the Cline and Cline Pass providers. The provider argument was ignored, so in Langfuse-configured environments prompts and responses from third-party and BYOK providers were exported too</li>
<li>OpenCode Go models are now routed to the right wire protocol. One base URL serves several protocols, but every model was sent over the OpenAI chat-completions adapter, so Anthropic-protocol and OpenAI Responses models on that endpoint failed or misbehaved. Requests also now carry the sticky session header and a Cline user agent</li>
<li>Claude Code and OpenCode are now declared local-auth providers with a named local CLI, so hosts route them to a local-CLI readiness screen instead of demanding an API key they never read — previously the workaround was storing a dummy key, and the desktop app rendered an OAuth button for OpenCode that did nothing</li>
<li>The OpenAI Codex (ChatGPT subscription) model list no longer gets overwritten with the full OpenAI API catalog by hosts that query provider models, which also lost the Codex context caps. The Codex backend's 400K/272K/128K budget is now clamped onto every Codex model rather than just one, so newer models no longer inherit the API catalog's 1M limits and produce over-long requests and wrong context math. Retired ChatGPT-account models were dropped from eligibility and the default moved to <code>gpt-5.6-terra</code></li>
<li>Live model catalogs now refresh for every shared-catalog provider, not just Cline and Cline Pass — other providers' lists were whatever was bundled at build time, so newly published models never appeared without a package update. The catalog fetch and the model-source fetch are both bounded by timeouts, so a hung models endpoint can no longer stall the provider list indefinitely</li>
<li>Providers are now reported as configured based on the provider their credentials are stored under. Cline Pass signs in as Cline and never writes its own entry, so a new Cline Pass user saw only one configured provider despite one sign-in configuring both. The "(free)" suffix now also applies to free-tier models whose ids come from upstream rather than Cline's own namespace</li>
<li><code>@cline/ui</code> gains a shared <code>Switch</code> built on a native checkbox, and <code>SearchCombobox</code> keeps section headers visible while searching and can refresh its options each time it opens. Migrators should note the <code>Switch</code> has no <code>data-state</code>/<code>asChild</code> hooks and tests must read the native <code>checked</code> property rather than <code>aria-checked</code></li>
<li>Cline Pass now defaults to a subscribed-tier model. Its provider list mixes subscribed (<code>cline-pass/*</code>) and free models, and the default was simply the first entry in a release-date-ordered list, so it drifted onto whichever free model had shipped most recently — a subscriber who never picked a model was put on the free tier rather than the one they pay for. The default is now restricted to <code>cline-pass/*</code> ids</li>
<li>Refreshed the model catalog. Adds four providers (Infer by Flow7, Melious, NaN, and Wallaby) and takes the bundled catalog from 5,788 to 6,079 models. This is a wide refresh: the resolved default model changes for 44 providers, most of them landing on DeepSeek V4.1 Flash — among them Hugging Face, Fireworks, Requesty, Nebius, Cortecs, CrossModel, DigitalOcean, Eden AI, and OpenCode Go. Gemini and Vertex now resolve to Gemini 3.8 Flash, GitHub Copilot and Vivgrid to GPT-6 Astra, and NVIDIA to GLM 5.3 Flash. If you use any provider without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.82...sdk/sdk/v0.0.83"><tt>sdk/sdk/v0.0.82...sdk/sdk/v0.0.83</tt></a></p>]]>
    </description>
    <content:encoded><![CDATA[<ul>
<li>Hub-managed Agent Plugins. Packages under <code>~/.agents/plugins/*</code> on the hub host are discovered and validated from their root <code>plugin.json</code>; valid skills under <code>skills/</code> are exposed through the skills tool as <code>plugin-name:skill-name</code>, and stdio, Streamable HTTP, and legacy SSE servers from <code>mcp.json</code> are started without touching <code>cline_mcp_settings.json</code>. Workspace <code>.agents/plugins</code> directories are deliberately not scanned, so opening a repo cannot implicitly start repo-controlled MCP servers; extra roots require an explicit <code>agentPluginPaths</code>. Enablement lives in hub settings keyed by manifest name and publishes <code>settings.changed</code>, so clients no longer need their own loader or enablement store. Two bugs fixed along the way: <code>settings.toggle({type: "skills"})</code> wrote a <code>disabled</code> key into a plugin skill's SKILL.md frontmatter, which the strict Agent Skills parser then rejected so the skill silently vanished until hand-edited; and <code>InMemoryMcpManager.dispose()</code> aborted on the first <code>disconnect()</code> rejection, leaking every remaining server's process</li>
<li>A model turn that dies mid-stream with a transient provider error is now retried up to 3 times with exponential backoff instead of failing the whole run — a single forwarded 429 previously aborted the run outright. Retryability is read from the AI SDK's typed signals, and a turn is never retried once it has streamed any text, reasoning, media, or tool call, so nothing is duplicated. Model calls also now allow 5 SDK-level retries for request-start 429/5xx/network failures, up from 2</li>
<li>Streaming is no longer throttled by hook forwarding. The hub proxied every runtime event to client-contributed <code>onEvent</code> hooks as a capability round trip carrying the full session snapshot, with the agent loop awaiting it — so each streamed token cost a few hundred KB of serialization, a persisted row, and a blocking IPC hop. Per-chunk text, reasoning, and tool-update deltas are no longer forwarded to remote <code>onEvent</code> hooks; every other event still reaches hooks unchanged. The hub event log also moved to <code>synchronous = NORMAL</code>, dropping one fsync per appended delta</li>
<li>Checkpoints no longer re-hash unchanged untracked files on every turn. Each turn built a throwaway git index, so git re-read every untracked file before each model call — with multi-GB untracked data in the workspace this blocked every message for seconds to minutes. A persistent per-session snapshot index lets git's stat cache skip unchanged files, so from the second turn a snapshot costs about one git process. Snapshot contents are byte-identical to before. A corrupt index now heals with one rebuild-and-retry that also clears a stale <code>index.lock</code>, where previously a git process killed mid-add degraded that session to HEAD-only checkpoints permanently</li>
<li><code>run_commands</code> no longer hangs when a command backgrounds a child. The executor settled on the child's <code>close</code> event, which waits for the stdio pipes to drain; a backgrounded process (<code>cmd &amp;</code>, <code>nohup</code>) holds the inherited write-ends open, so a finished command hung until the timeout killed the whole process tree. It now also settles on <code>exit</code> after a one-second grace period, reporting the exit code and the output collected so far</li>
<li><code>apply_patch</code> "Add File" against a path that already exists is now rejected instead of silently overwriting it. Only UPDATE and DELETE targets were pre-loaded, so the parser's "File already exists" guard never saw ADD targets and the patch destroyed the file's contents with no error and no diff of what was lost</li>
<li>Nested PowerShell invocations are now unwrapped instead of being double-parsed. Command text is fed to PowerShell through a stdin bootstrap that the outer shell parses as PowerShell source, so a command written as <code>powershell -Command "... $_ ..."</code> had its argument interpolated before the nested shell ran — pipelines using <code>$_</code> emitted one error per enumerated item, an error flood that looked like a hang, while the child still exited 0. Unwrapping is applied only when it is semantics-preserving: same PowerShell edition, <code>-NoProfile</code>, and an entirely quoted <code>-Command</code> tail; everything else passes through byte-identical</li>
<li>The <code>run_commands</code> tool description now names the actual PowerShell edition in use — <code>Windows PowerShell (powershell.exe)</code> versus <code>PowerShell (pwsh.exe)</code> or <code>PowerShell (pwsh)</code> — quotes its guidance against the resolved executable, and tells the model to write commands directly rather than wrapping them in another <code>-Command</code> or <code>/c</code> invocation. It also no longer claims "in Windows environment" when <code>pwsh</code> is the configured shell on macOS or Linux</li>
<li>No file index is built when the workspace root is the home directory or a filesystem root. Running from <code>$HOME</code> and typing an <code>@</code> mention listed every file under home and re-ranked the whole index on each keystroke, driving the process to many GB of RSS until it was OOM-killed. Paths are canonicalized first so symlinked or differently-cased spellings still hit the guard</li>
<li>Credential fields are now stripped of Unicode control and format characters and surrounding whitespace before being saved. A key pasted with an invisible character (BOM, zero-width space, bidi mark) was stored corrupted and the provider returned a 401 indistinguishable from a genuinely wrong key, while masked rendering hid the corruption</li>
<li>The <code>editor</code> tool's error for a null <code>old_text</code> now names the file, says whether the parameter was null or omitted, and spells out the recovery. Models that fill optional parameters with null hit a terse "required" message and re-sent the identical call until the loop detector hard-stopped the run</li>
<li>Provider-native web search is now enabled by default in non-yolo sessions on supported provider/model combinations, instead of requiring <code>tools.web_search.enabled = true</code>. An explicit <code>false</code> still opts out, and the settings loader fails closed: web search stays disabled when a global settings file exists but cannot be read or parsed</li>
<li>Session history no longer hides root sessions behind their children. The list over-fetched a fixed window of rows and filtered out subagent and team-task children client-side; since children sort after the root that spawned them, one session with more children than the window hid itself and every older root. Listing now widens until the requested page fills, and the backends filter child rows in SQL</li>
<li>Schedules no longer stall each other. The cron runner awaited the whole claimed batch, so one long agent turn blocked dispatch of every other schedule; dispatch is now decoupled from execution, and per-schedule and global parallelism are enforced inside the SQLite claim transaction so multiple connections cannot exceed a schedule's limit. Locally active claims are renewed each tick, so work resumed after system sleep is not reclaimed and started twice, and terminal status is written before the markdown report so a failed report write cannot replay a completed turn</li>
<li>Recurring schedules created without a timezone now persist the host's local IANA timezone rather than an implicit default, and the scheduling tool prompts tell the model to omit the timezone instead of guessing</li>
<li>Automation event acceptance is now atomic. The event log row was committed before specs were matched and runs materialized, so a failure mid-fan-out left the event permanently deduplicated and impossible to redeliver, and other connections could observe partially materialized runs. Acceptance now runs in one write transaction, and failures roll back and propagate to the caller for redelivery</li>
<li>New <code>RemoteEnvironmentService</code> runs sessions on an SSH host while the client stays local. <code>connect()</code> inspects the remote host, uploads a self-contained helper binary to <code>~/.cline/remote/</code>, starts a loopback-bound Hub there, and opens an SSH tunnel to it; the returned endpoint and token are passed to <code>ClineCore.create({ backendMode: "remote" })</code>, after which tools, sessions, and persistence all execute remotely. SSH runs with <code>BatchMode=yes</code> and <code>StrictHostKeyChecking=yes</code>, so the host key must already be known and password authentication is not supported; profiles are stored at mode 0600 and hold identity-file paths only, never secrets. Remote targets are Linux and macOS on x64/arm64, and embedders must supply a helper binary cross-compiled for the target. This is an SDK building block — no CLI command or desktop UI is wired to it yet, and it is inert unless an embedder constructs the service</li>
<li>A session started without an explicit <code>systemPrompt</code> now gets a default Cline prompt built on the execution host, rather than running with none. Callers that pass their own prompt are unaffected</li>
<li>Hub daemons can now be started with connector management disabled, so an SSH-spawned Hub does not adopt or restart the account's Slack and Telegram connectors. Locally started hubs are unaffected</li>
<li>Hub clients can now authenticate a remote connection with HTTP headers via a <code>resolveConnectionHeaders()</code> resolver, re-resolved on every reconnect so short-lived credentials can refresh. It is mutually exclusive with the local hub-token subprotocol. <code>NodeHubClient.connect()</code> was also reworked so concurrent calls share one in-flight promise and a stale registration or connect timeout can no longer clobber a newer attempt</li>
<li>A hub running the same core version as the client no longer prompts to update. Two artifacts of the same release cut from different commits never share a build fingerprint, so anyone with both the desktop app and the CLI installed got a "Cline Hub was updated" dialog on every launch whose "Update and restart" looped on "no app update available". Genuinely different core releases still prompt</li>
<li>Sessions seeded with history (forks, checkpoint restores) now persist their live idle status instead of defaulting to "running", which left a restored session showing as busy forever</li>
<li>Sessions imported from Claude Code, Codex, or opencode now summarize the foreign history on first resume rather than replaying tool calls the current agent cannot make. The summary is persisted so it runs once, the canonical transcript is left intact, and a failed attempt falls back to the raw history. Imported sessions also record an import origin that is stamped on their telemetry, and resuming a session no longer overwrites stored import or automation provenance with a default "user" origin</li>
<li>Session import source directories now resolve correctly on Windows profiles where <code>USERPROFILE</code> disagrees with what Node reports, and honor <code>CLAUDE_CONFIG_DIR</code>, <code>CODEX_HOME</code>, and <code>XDG_DATA_HOME</code></li>
<li>Cline Pass and free models now report zero cost. The backend reports upstream market cost on those responses and it was passed straight through, so subscription and free usage showed dollar figures for requests that are not billed per token</li>
<li>Offline clients now fall back to generated Recommended, Free, and Subscribed model lists built at release time. The fallback was a hand-maintained literal of six models with an empty Cline Pass tier, so when the recommended-models endpoint was unreachable, subscribers got no subscribed tier at all</li>
<li>Langfuse tracing is now limited to the Cline and Cline Pass providers. The provider argument was ignored, so in Langfuse-configured environments prompts and responses from third-party and BYOK providers were exported too</li>
<li>OpenCode Go models are now routed to the right wire protocol. One base URL serves several protocols, but every model was sent over the OpenAI chat-completions adapter, so Anthropic-protocol and OpenAI Responses models on that endpoint failed or misbehaved. Requests also now carry the sticky session header and a Cline user agent</li>
<li>Claude Code and OpenCode are now declared local-auth providers with a named local CLI, so hosts route them to a local-CLI readiness screen instead of demanding an API key they never read — previously the workaround was storing a dummy key, and the desktop app rendered an OAuth button for OpenCode that did nothing</li>
<li>The OpenAI Codex (ChatGPT subscription) model list no longer gets overwritten with the full OpenAI API catalog by hosts that query provider models, which also lost the Codex context caps. The Codex backend's 400K/272K/128K budget is now clamped onto every Codex model rather than just one, so newer models no longer inherit the API catalog's 1M limits and produce over-long requests and wrong context math. Retired ChatGPT-account models were dropped from eligibility and the default moved to <code>gpt-5.6-terra</code></li>
<li>Live model catalogs now refresh for every shared-catalog provider, not just Cline and Cline Pass — other providers' lists were whatever was bundled at build time, so newly published models never appeared without a package update. The catalog fetch and the model-source fetch are both bounded by timeouts, so a hung models endpoint can no longer stall the provider list indefinitely</li>
<li>Providers are now reported as configured based on the provider their credentials are stored under. Cline Pass signs in as Cline and never writes its own entry, so a new Cline Pass user saw only one configured provider despite one sign-in configuring both. The "(free)" suffix now also applies to free-tier models whose ids come from upstream rather than Cline's own namespace</li>
<li><code>@cline/ui</code> gains a shared <code>Switch</code> built on a native checkbox, and <code>SearchCombobox</code> keeps section headers visible while searching and can refresh its options each time it opens. Migrators should note the <code>Switch</code> has no <code>data-state</code>/<code>asChild</code> hooks and tests must read the native <code>checked</code> property rather than <code>aria-checked</code></li>
<li>Cline Pass now defaults to a subscribed-tier model. Its provider list mixes subscribed (<code>cline-pass/*</code>) and free models, and the default was simply the first entry in a release-date-ordered list, so it drifted onto whichever free model had shipped most recently — a subscriber who never picked a model was put on the free tier rather than the one they pay for. The default is now restricted to <code>cline-pass/*</code> ids</li>
<li>Refreshed the model catalog. Adds four providers (Infer by Flow7, Melious, NaN, and Wallaby) and takes the bundled catalog from 5,788 to 6,079 models. This is a wide refresh: the resolved default model changes for 44 providers, most of them landing on DeepSeek V4.1 Flash — among them Hugging Face, Fireworks, Requesty, Nebius, Cortecs, CrossModel, DigitalOcean, Eden AI, and OpenCode Go. Gemini and Vertex now resolve to Gemini 3.8 Flash, GitHub Copilot and Vivgrid to GPT-6 Astra, and NVIDIA to GLM 5.3 Flash. If you use any provider without pinning a model, expect a different default</li>
</ul>
<p><strong>Full Changelog</strong>: <a class="commit-link" href="https://github.com/cline/cline/compare/sdk/sdk/v0.0.82...sdk/sdk/v0.0.83"><tt>sdk/sdk/v0.0.82...sdk/sdk/v0.0.83</tt></a></p>]]></content:encoded>
    
    <pubDate>2026-09-15T05:53:27.000Z</pubDate>
  </item><item>
    <title><![CDATA[/tmp/files暫存檔案平台：檔案title.txt提供下載，將於59分鐘後過期失效]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fllms%2Fv0.0.83</link>
    <itunes:title><![CDATA[/tmp/files暫存檔案平台：檔案title.txt提供下載，將於59分鐘後過期失效]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/llms@0.0.83</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/llms@0.0.83</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/llms@0.0.83</p>]]></content:encoded>
    
    <pubDate>2026-09-15T05:53:11.000Z</pubDate>
  </item><item>
    <title><![CDATA[tmpfiles臨時空間提供「title.txt」下載，檔案將於59分鐘後過期]]></title>
    <link>https://github.com/cline/cline/releases/tag/sdk%2Fcore%2Fv0.0.83</link>
    <itunes:title><![CDATA[tmpfiles臨時空間提供「title.txt」下載，檔案將於59分鐘後過期]]></itunes:title>
    <itunes:author><![CDATA[github-actions[bot]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>@cline/core@0.0.83</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>@cline/core@0.0.83</p>]]>
    </description>
    <content:encoded><![CDATA[<p>@cline/core@0.0.83</p>]]></content:encoded>
    
    <pubDate>2026-09-15T05:53:14.000Z</pubDate>
  </item></channel>
</rss>